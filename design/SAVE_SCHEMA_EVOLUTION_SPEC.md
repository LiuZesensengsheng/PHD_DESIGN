# Save Schema Evolution Spec

Status: superseded by
`docs/development/architecture/SAVE_RESET_POLICY_V1.md`.

The previous version of this document treated historical save loading as the
default goal. That is no longer the active policy during the current pre-content
refactor window.

Use `Save Reset Policy V1` for current save compatibility decisions. Add a new
versioned migration policy only if the project later decides to preserve a real
external save corpus.

