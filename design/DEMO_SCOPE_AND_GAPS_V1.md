# Demo 版本范围与差距复盘（Draft）

> 用来对齐：**我们现在已经有什么？Demo 想做到什么？中间还差哪几步？**  
> 你可以直接在这个文档上删改、补充，最后定成自己的 Demo 规划。

---

## 1. Demo 目标（你可以改）

- **体验时长**：大约 30–60 分钟，可在一两局内完整体验一条小循环。
- **核心内容**：
  - 一条精简的 **战役甘特路径**（从开局 → 数场战斗/事件 → 结尾）。
  - 至少 3–5 个有特色的 **敌人**（包含 1 个精英/小 Boss）。敌人设计了5个，只适配一个战斗，其他的战斗场景还没有做。 boss没做
  - 一套可玩的 **卡组 + 白色扩展**（已经基本具备）。
  - 1 次可感知的 **组会/Meeting**（升级/删牌/购卡 + 1–3 条事件）。组会有事件了，但是很粗制滥造
  - 几个 **生活梗事件**（气泡或对话形式，让玩家看到博士日常）。 有了，但是很粗制滥造
- **调性**：以 **“扎心 + 爽 + 反思”** 为主，发疯度控制在 0–1 之间，少量 2 级作为彩蛋。

---

## 2. 当前状态快速总结

- **战斗系统（Combat）**
  - 已有：基础卡组 + 白色扩展卡、多数效果实现、压力/自信、状态、指针等核心机制。
  - 问题：**缺少“填好的敌人数据”**（敌人 JSON / 技能列表 / 数值带）。

- **敌人设计（Enemies）**
  - 已有：
    - 颜色哲学 + Act1 阵容纲要（`ACT1_ROSTER`）。
    - `LIFE_EVENTS_ENEMIES_V1` 中 ~30 条生活事件敌人原型。
    - 意图系统规范（`INTENT_SPEC`）+ 意图目录（`INTENTS_CATALOG`）。
    - 一份 csv 索引：`docs/design/enemies_life_events_v1_index.csv`（12 个代表敌人 + 情绪功能 + 发疯度）。
  - 问题：这些设计 **还没有被转写成真正可用的敌人数据表/JSON**。

- **战役甘特（Campaign + Gantt）**
  - 已有：完整的甘特 UI 与逻辑、教程 Questline（从主菜单→战役→首战）。
  - 问题：**没有一条“专为 Demo 设计的短路线”**（只跑几场战斗/事件就结束的那种）。是的

- **组会 / 经济（Meeting & Inspiration）**
  - 已有：Inspiration 货币、MeetingService、MeetingScreen UI，升级/删牌流程，大部分逻辑与测试。
  - 问题：Demo 视角下，只需要确认：
    - 至少一次组会可以正常出现；正常
    - 升级/删牌/购卡能走通； 购买没做
    - Meeting 事件池内有 1–3 条事件挂上 UI。 当前我们的ddl战斗是错的

- **事件 / 段子（Events & Bubbles）**
  - 已有：大量事件文案（通用事件、Meme 事件、助教支线等），以及甘特气泡/Meeting 事件的设计规范。
  - 问题：**缺少“真正会在 Demo 中出现的那几条被选中的事件”**，以及它们与系统的挂钩。 是的

- **理想 / 审判 / Excel Hell**
  - 已有：详细设计稿与部分逻辑雏形。有理想过程，但是效果没有实现，缺少数值和效果
  - 建议：**作为 Plus，要么做成彩蛋/演出，要么暂时不强求进入 Demo 主流程**。
 然后我们做了音效了
 红色卡组也基本设计完，等待实现。
---

## 3. 面向 Demo 的关键差距（精简版 TODO，大方向）

> 这里只列“为了一个可玩的 Demo 必须补上的坑”，不是全项目 TODO。  
> 你可以直接在每条后面打勾、补充子任务。

- **Gap 1：从设计到数据的 3–5 个敌人原型**
  - 从 `enemies_life_events_v1_index.csv` / `LIFE_EVENTS_ENEMIES_V1` 中，确定 Demo 期的 **3–5 个代表敌人**：
    - 普通敌人 2–3 个（如：昨天的 DDL、拖延菌群、财务大妈、闹钟…）。
    - 精英/小 Boss 1–2 个（如：查重红条、甲方的折磨、论文开头退错了）。
  - 为每个代表敌人写一份 **正式原型表**（可以基于 `TEMPLATE_ENEMY_PROTO`）：
    - HP / 颜色 / 角色定位。
    - 2–4 个技能，对应 `INTENTS_CATALOG` 里的意图条目。
    - 粗略数值带（普通/精英层级）。
  - 把这些原型映射到 **JSON / 数据文件**，接入当前 Enemy/Intent 系统。

- **Gap 2：一条“Demo 专用甘特路线”**
  - 设计一条很短的战役路径，例如：
    - 开局 → 普通战斗 → 事件或休息 → 普通战斗 → 组会 → 精英战/小 Boss → 结束结语。
  - 在数据/配置里为这条路线单独标记（方便 Demo 模式直接跳用）。
  - 确保：
    - 每个关键节点都能顺利进入/退出；
    - 不会卡在尚未完成的系统（例如尚未正式启用的大型剧情）。

- **Gap 3：组会的 Demo 级别体验**
  - 为 Demo 锁定一版最小可玩集：
    - 免费升级 ×1（可使用一次，逻辑完整）。
    - 升级/删牌 流程走通（UI + 持久化）。
    - 购卡 → 通过 RewardService 弹出一次简单卡牌选择。
  - Meeting 事件：
    - 从现有导师/助教事件中选出 1–3 条，接入 `MeetingEventModal`。
    - 使用当前 Condition/Effect 体系的最小子集（灵感增减、新任务线标记等）。

- **Gap 4：5–8 条“真的会出现在 Demo 里”的事件**
  - Gantt 气泡事件：
    - 挑选 3–5 条纯风味或轻效果事件（黑色幽默段子），挂在甘特气泡系统上。
  - Meeting 事件：
    - 与 Gap 3 复用/合并，确保 Demo 至少会遇到 1–2 次有选择的组会对话。
  - 这些事件不需要复杂长链，只要：
    - 看起来有梗；
    - 选项有清晰差别；
    - 结果在数值/卡组上有一点点实际反馈。

- **Gap 5：一轮“Demo 视角”的整体检查**
  - 数值层面：敌人 HP/伤害、卡牌强度在 Demo 难度下不过分离谱。
  - 体验层面：从开始游戏到 Demo 结束，**没有明显“这里是半成品”的断层**。
  - 表达层面：关键节点的文字（敌人名称、事件标题、选项文案）与系统行为一致，不误导玩家。

---

## 4. 你可以怎么用这个文档

- 当作 **“Demo 专用总纲”**：  
  - 觉得哪段描述不对，就直接改文字；  
  - 觉得 Gap 列错了，就删或拆小。
- 当作 **“给未来合作者看的入口”**：  
  - 对方只要读完本文件，就能理解：  
    - 现在有哪些模块是“基本完成”的；  
    - 哪些是“只差几步能变成 Demo”的；  
    - 哪些是“完整版才考虑的远景设计”。

你可以先随便在这里标几处“优先级高/低”、或者在 Gap 后面写上“我来 / 交给别人”，等你哪天不那么累了，再回来细化每一条也完全来得及。


