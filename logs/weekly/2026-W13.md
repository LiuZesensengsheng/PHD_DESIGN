# Weekly Summary

- Week: `2026-W13`
- Source Daily Logs:
  - `docs/logs/daily/2026-03-29.md`

## Week Focus

- Land `Combat Compat-Zero Plan V1` from Player/public wrappers through EffectExecutor mainline naming
- Move combat ordering ownership away from compat hosts and into explicit orchestrators/dispatchers
- Establish a stable gate for future card/enemy expansion without reintroducing legacy seams

## Major Progress

- Completed Phase A:
  - removed Player public compat wrappers from combat mainline
  - migrated timing/event contract tests to orchestrator/dispatcher-driven semantics
- Completed Phase B:
  - removed legacy `on_*` hook API from combat trait/power types
  - tightened trait/power hook gate to zero-tolerance
- Completed Phase C:
  - enemy passive runtime is now `windows`-native and dispatcher-owned
  - removed enemy passive `hook` runtime semantics and migrated checked-in enemy JSON to `windows`
- Completed Phase D:
  - renamed EffectExecutor mainline fallback entrypoints to neutral APIs
  - removed old EffectExecutor API names from orchestrator/mainline call-sites
- Completed Phase E:
  - promoted the active gate to `COMBAT_MAINLINE_GATE_V2`
  - added `python scripts/check_combat_compat_zero.py` as a direct pre-expansion guard

## Key Decisions

- Compat-zero is enforced at mainline call-sites first, with any remaining shims isolated and explicitly guarded.
- Enemy passives now enter combat strictly through `windows` + dispatcher ordering, not through enemy-owned runtime hook paths.
- Effect fallback entrypoints are treated as naming/ownership contracts, not just helper aliases.

## Current Blockers

- A few localized compatibility shims still physically exist:
  - Player failure stubs in `player.py`
  - old-name wrapper methods in `EffectExecutor`
- Some runtime behavior still uses queue-external fallback paths, even though those paths now use the neutral API and no longer own combat ordering semantics.

## Next Default Direction

- Build the next migration pass from `COMBAT_FALLBACK_AND_SHIM_INVENTORY_V1.md`
- Decide whether to hard-delete localized shims or keep them through one more expansion cycle under V2 gating
- Continue expanding combat content only on top of window-native, event-native, `windows`-native, and neutral fallback APIs

## Promotion Suggestions

- Promote `COMBAT_MAINLINE_GATE_V2.md` as the default combat extension gate for future work
- Keep `scripts/check_combat_compat_zero.py` in default command memory so pre-expansion checks stay cheap and repeatable
