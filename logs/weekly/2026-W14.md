# Weekly Summary

- Week: `2026-W14`
- Source Daily Logs:
  - `docs/logs/daily/2026-03-30.md`
  - `docs/logs/daily/2026-03-31.md`
  - `docs/logs/daily/2026-04-01.md`
  - `docs/logs/daily/2026-04-03.md`
  - `docs/logs/daily/2026-04-04.md`

## Week Focus

- Hard-delete the last localized combat compat shims and tighten the active V2 gate from "isolated seams allowed" to physical zero definitions plus zero usages.
- Push active combat mainline ownership to queue-only card-play and top-of-draw resolution, keeping the checked-in `red/white` fallback boundary at `0`.
- Extend the same cleanup mindset beyond combat by removing campaign and judgment paths that silently reinterpret invariant failures as normal game outcomes.

## Major Progress

- Physically deleted the remaining Player-local and old-name `EffectExecutor` compat shims, then updated the combat gate, inventory, and precheck docs/scripts to treat those seams as fully removed baseline behavior.
- Completed the non-visual combat cleanup pass that centralized shared effect-resolution sequencing, cleared the active fallback scan to zero, and added minimal planner/action support for `swap_with_paper`, `pointer_step`, `add_steadfast_token`, and `add_paradigm`.
- Removed the remaining combat runtime fallback seams around card-play/top-card resolution, pointer-target inference, headless simulation configuration, enemy damage commit, enemy-turn step continuation, filtered intent selection, heart-demon runtime card building, and selected-enemy default targeting.
- Kept the combat baseline green through focused gates plus broader regressions: `check_combat_compat_zero.py`, the allowlist gate, `tests/combat`, `tests/simulation`, data-pipeline checks, and encoding guards all stayed passing after the cleanup.
- Tightened campaign and judgment failure contracts so thesis judgment, meeting event selection, DDL snake progression, campaign turn advance, trait purchase persistence, failed meeting event retries, and stress judgment no longer pretend success or silently continue after structural failures.

## Key Decisions

- Treat physical compat-shim deletion as the new combat baseline; future runtime work should start from planner/action modeling and explicit queue ownership rather than wrapper compatibility.
- Card-play and top-of-draw combat mainline are now queue-only; surviving direct `EffectExecutor` helpers are test/tool-only and should not flow back into runtime ownership.
- When runtime invariants break, prefer explicit errors, rollback, or deterministic failure states over fabricated "normal" outcomes that hide corruption.

## Current Blockers

- No hard delivery blocker was recorded this week, but the next combat non-visual slice is still undecided between activating `white_design_v2` in the runtime load path or implementing the first real `paradigm` / `steadfast` trigger semantics.
- Campaign still needs a deliberate scan for any remaining `warn and continue` or fabricated-success paths so the new failure-contract tightening becomes a stable cross-cutting rule instead of a one-off cleanup batch.
- Meeting-event retry behavior is now structurally safer, but the UI-side retry experience still needs a small follow-up so preserved failed events can be retried cleanly.

## Next Default Direction

- Keep future combat effect families behind planner/action contracts and treat `QueueOnlyPlanningError` as a modeling signal, not a reason to add a new silent fallback seam.
- Choose one narrow combat follow-up slice: either activate `white_design_v2` in runtime loading or implement a single concrete trigger window for `paradigm` / `steadfast`.
- Continue the fallback-removal pass on the campaign side by scanning for remaining progression-mutating failure paths and deciding which thesis/campaign compatibility wrappers can now be hard-removed.
- If meeting-event retry remains part of the intended UX, add the minimum UI seam needed to retry the preserved active event without reopening it through a separate flow.

## Promotion Suggestions

- If the same cleanup pattern continues next week, promote a broader engineering guardrail that runtime/domain code should fail closed on invariant breaks instead of fabricating normal gameplay outcomes.
- After the campaign-side scan stabilizes, consider writing a short long-term note that mirrors the combat gate philosophy for campaign/thesis failure contracts.
- No additional promotion is needed for combat queue-only status this week because the active combat gate and fallback-inventory docs were already updated during the cleanup.
