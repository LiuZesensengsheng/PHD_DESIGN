# Daily Log

- Date: `2026-04-27`
- Active branches:
  - `codex/04-27-power-curve-model-lab`
  - `codex/04-27-evaluation-design-model-lab`
  - `codex/04-27-gossip-bbs-model-lab`

## Today Goals

- Establish long-running model labs for campaign pacing, evaluation/autonomous-design,
  and the future in-game gossip/BBS system.
- Keep all three lanes bounded: report-only or sidecar-shaped, no hard gates, no
  recommendation default changes, no persistence/UI/LLM prose integration unless a
  later owner seam is explicit.

## Completed

- Campaign power curve:
  - Defined `campaign_power_curve_model_v1` and later
    `campaign_power_curve_report_v1` as report-only planning/context surfaces.
  - Added phase bands, checkpoint evidence, source-support splits, reward/encounter
    support visibility, and advisory `authority_boundary=advisory_context_only`.
  - Registered the surface and wired it into evidence bundle/default-entrypoint memory.
  - Kept monster stats, hard counters, pass/fail authority, and runtime encounter
    implementation out of scope.
- Evaluation autonomous design:
  - Defined and implemented `evaluation_autonomous_design_model_v1` as a report-only
    orchestration layer.
  - Normalized inputs across mechanism-axis discovery, fun/health, deck compression,
    package health, design iteration, and campaign context without replacing their
    canonical owners.
  - Added fixtures, CLI, registry/default-entrypoint memory, and focused tests.
- Gossip/BBS social simulation:
  - Started `gossip_bbs_social_simulation_model_v1` as a sidecar-shaped domain model.
  - Added typed event and transition contracts, typed output/result payloads, core
    records, and in-memory `GossipSimulationState`.
  - Added the first-slice fixture pack and tests under
    `tests/gossip_bbs_social_simulation/`.
  - Modeled early event slices including public achievement reinforcement,
    package-evidence theorycraft, rumor seeding/debunking, misunderstanding
    reactivation, private leak moderation, scandal escalation, combat-result memory
    rebalance, boss-defeat milestone threads, archive/decay, and reply pressure.

## Validation

- Campaign/evaluation lanes used their owning evaluator, CLI, registry, evidence
  bundle, architecture, and encoding tests as recorded in their branch handoffs.
- Gossip/BBS model validation:
  - `py -3.11 -m pytest tests/gossip_bbs_social_simulation -q`

## Current Blockers

- No hard blocker.
- Campaign power curve still needs main-agent integration review before it informs
  encounter validation.
- Evaluation autonomous design remains report-only and should not generate cards or
  mutate candidates.
- Gossip/BBS remains sidecar-shaped until a campaign-owned lifecycle seam is chosen.

## Next Step

- Merge model-lab outputs through the main integration branch one lane at a time.
- For gossip/BBS, next work should introduce a campaign-owned adapter/service seam
  without touching UI, save/persistence, real prose generation, or cardanalysis.

## Candidate Long-Term Memory

- `campaign_power_curve_report_v1` is advisory campaign pacing context, not monster
  stat tuning authority.
- `evaluation_autonomous_design_model_v1` orchestrates existing report-only summaries
  but does not own their scoring semantics.
- `gossip_bbs_social_simulation_model_v1` owns social-state transition semantics and
  should remain importable without campaign runtime dependencies.

## Session: `codex/04-27-asset-file-architecture`

- Branch: `codex/04-27-asset-file-architecture`

## Today Goals

- Turn the recent asset/file-layout discussion into a stable long-term document.
- Clarify the boundary between runtime assets in Git and editable source art in
  an external synced directory.
- Lock the intended semantics of `scratch/` vs `tmp/` and shared-code vs shared-media.

## Completed

- Added `docs/development/architecture/PROJECT_FILE_LAYOUT_V1.md` as the target file-layout
  and migration guide for:
  - Unreal-inspired content governance without adopting the full Unreal engine
    object model
  - runtime assets under `assets/`
  - external `PHD_SIMULATER_ART/` as the source-art home
  - manifest-based approved runtime asset mapping
  - `contexts/shared/` as shared code only
  - top-level `tests/`
  - `scratch/` vs `tmp/`
  - optional `skills/` positioning
- Added a small `docs/README.md` discoverability note for the new file-layout rule.

## Current Blockers

- No hard blocker was recorded for this session.

## Next Step

- Decide whether to also record this layout as an accepted decision in
  `docs/pm/DECISION_LOG.md`.
- Add a lightweight local-path convention for the external art root
  (`config/local_paths.local.json` or env var).
- Add manifest and path guardrails once the first migration slice begins.

## Candidate Long-Term Memory

- `docs/development/PROJECT_FILE_LAYOUT_V1.md` is the current source of truth for
  runtime asset placement, external source-art boundaries, top-level test layout,
  and `scratch/` vs `tmp` semantics.

## Current Status

- The repo now has an explicit target architecture for file placement and asset-source separation.
- The document is intentionally written as an end-state plus phased migration
  guide rather than a one-shot rename plan.

## Next Likely Move

1. Decide whether to also record this layout as an accepted decision in
   `docs/pm/DECISION_LOG.md`.
2. Add a lightweight local-path convention for the external art root
   (`config/local_paths.local.json` or env var).
3. Later, add manifest and path guardrails once the first migration slice begins.