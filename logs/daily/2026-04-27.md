# Daily Log

- Date: `2026-04-27`
- Active branches:
  - `codex/04-27-power-curve-model-lab`
  - `codex/04-27-evaluation-design-model-lab`

## Campaign Power Curve Workstream

### Today Goals

- Start the Campaign Power Curve Model Lab from latest `origin/master`.
- Keep the first run docs-only and report-only.
- Define a minimal `campaign_power_curve_model_v1` contract without touching hard
  gates, recommendation defaults, learned/reranker paths, or monster implementation.

### Completed

- Created `docs/exploration/CAMPAIGN_POWER_CURVE_LAB.md`.
- Established the first model vocabulary for:
  - `campaign_round`
  - `player_power_state`
  - `deck_maturity_state`
  - `mechanism_online_state`
  - `economy_state`
  - `compression_state`
  - `enemy_pressure_profile`
  - `encounter_archetype`
  - `curve_checkpoint`
- Added report-only player strength and enemy pressure vectors.
- Added initial phase labels: `starter`, `early`, `build`, `pivot`, `mature`, `late`.
- Added first transition assumptions for power spikes, mechanism online timing,
  compression/removal effects, and enemy pressure by phase.
- Added a report-only payload draft, three encounter archetypes, and a future
  cardanalysis interface draft.
- Added the first checkpoint example suite:
  - `starter_round_1_baseline_survival_v0`
  - `build_round_5_identity_assembly_v0`
  - `pivot_round_8_conditional_online_v0`
  - `mature_round_12_ceiling_and_recovery_v0`
- Kept checkpoint examples qualitative and advisory-only, with `authority_boundary`
  and `evaluation_mode=report_only` visible in each record.
- Added `checkpoint_evidence_state` vocabulary so checkpoints can state whether they
  are `hypothesis_draft`, `source_aligned`, `reviewed_design`,
  `playtest_observed`, `stale_assumption`, or `superseded`.
- Added evidence-state rules for freshness, missing evidence, stale triggers, allowed
  use, and the `not_a_gate` authority boundary.
- Added `mechanism_online_timing` as a phase-aware report-only rubric.
- Mapped `absent`, `assembling`, `conditional_online`, `online`, and `over_online` to
  phase expectations, evidence needs, transition evidence, and common false positives.
- Added economy and compression online-timing modifiers for removal, upgrade, shop,
  transform, event, healing, persistent removal, in-combat exhaust, discard filtering,
  and draw selection.
- Kept modifier output advisory-only through labels such as `advance_online_claim`,
  `hold_conditional`, `delay_online_claim`, `route_dependent`, and
  `stale_check_needed`.
- Added modifier-driven encounter archetypes:
  - `build_payoff_only_detector`
  - `pivot_compression_route_probe`
  - `mature_over_online_texture_probe`
- Bound those archetypes to timing modifiers, qualitative pressure axes, validation
  questions, and explicit report-only interpretation boundaries.
- Added qualitative `enemy_pressure_phase_band` guidance for `avoid`, `trace`,
  `low`, `medium`, `high`, and `spike_only`.
- Added default phase-band guidance for `starter`, `early`, `build`, `pivot`,
  `mature`, and `late`, keeping the bands advisory and non-numeric.
- Added `player_power_reason_code` conventions so player strength vector changes can
  carry axis, polarity, evidence scope, phase relevance, and uncertainty.
- Added reason-code families for frontload, defense, draw, energy, compression,
  mechanism support, combo alignment, fail-state backup, and route uncertainty.
- Added `deck_maturity_state` labels covering starter-heavy, identity-hint,
  assembling, payoff-only, bridge-ready, pivot-ready, mature-core,
  goodstuff-resilient, and overfocused-brittle shapes.
- Added deck maturity dimensions for support/payoff density, bridge-before-payoff,
  starter pollution, off-axis drag, redundancy, and fallback plan.
- Added `economy_state` labels for upgrade breakpoints, removal routes, shop windows,
  event assumptions, transform quality, healing pressure, opportunity cost, economy
  surplus, and route risk.
- Clarified that economy labels modify online timing and route credibility; they do
  not estimate exact route probability or create readiness authority.
- Added `compression_state` labels for persistent removal, transform quality,
  self-exhaust setup, early/late targeted exhaust, discard filtering, draw selection,
  deck-size sensitivity, curated deck cleanup assumptions, and compression tax.
- Clarified that compression labels affect online timing and reachability wording
  only, not pass/fail authority.
- Added a `curve_checkpoint` review checklist covering checkpoint identity,
  authority boundary, phase fit, player reasons, deck maturity, mechanism timing,
  economy, compression, enemy pressure, encounter archetype link, cardanalysis inputs,
  staleness, and next evidence.
- Added checklist status labels such as `present`, `review_needed`,
  `reviewed_advisory`, and `stale_check_needed`.
- Added phase-specific report-only payload variants for starter baseline, build
  assembly, pivot route-dependent online, and mature ceiling/recovery contexts.
- Kept the payload variants qualitative and explicit about `must_not_expose`.
- Added a future cardanalysis field mapping draft for `deck_compression_report_v1`,
  `mechanism_axis_discovery_v1`, `card_package_health_v1`,
  `design_iteration_brief_v1`, `mechanism_fun_health_v1`, and
  `cardanalysis_evidence_bundle_v1`.
- Kept the mapping advisory-only and explicit that learned/reranker shadow output
  must remain default-off and non-authoritative.
- Added a lab backlog and review handoff section.
- Marked the exploration artifact as ready for main-agent review only in the
  docs-only/report-only sense, not runtime implementation.
- Promoted the stable implementation target into
  `docs/development/CAMPAIGN_POWER_CURVE_MODEL_V1.md`.
- Added report-only implementation:
  - `tools/combat_analysis/design_engine/campaign_power_curve_model.py`
  - `scripts/run_campaign_power_curve_report.py`
- Added reviewed fixtures and tests for the new surface:
  - `tests/fixtures/combat_analysis/campaign_power_curve_report_v1/`
  - `tests/toolkit/combat_analysis/test_campaign_power_curve_model_v1.py`
  - `tests/scripts/test_run_campaign_power_curve_report.py`
- Registered `campaign_power_curve_report_v1` in the report-only surface registry.
- Extended `cardanalysis_evidence_bundle_v1` to accept
  `campaign_power_curve_summary`.
- Calibrated phase/checkpoint `evidence_state` defaults against current campaign
  reward and encounter evidence.
- Added phase-specific `calibration_notes`, tighter `missing_evidence`, and a late
  `exploration_prompt` default so `evidence_refs` alone do not over-promote
  `early`/`build`/`pivot`/`mature`/`late` checkpoints to `source_aligned`.
- Updated reviewed campaign power curve fixtures to cite current reward, encounter,
  and block-shape sources for non-starter phases while keeping those checkpoints
  advisory.
- Recalibrated phase round bands against the current campaign board sequencing:
  `0-2 / 3-5 / 6-8 / 9-11 / 12-14 / 15+`.
- Grounded that calibration in the current startup seed and thesis blueprint cadence,
  both of which place primary board windows on `start_turn=0,3,6,9...`.
- Updated the `build`, `pivot`, and secondary `mature` fixture examples so their
  `round_index` values land inside the calibrated windows.
- Added `curve_checkpoint.evidence_state.source_support` to split support into
  `block_support`, `reward_support`, and `encounter_support`.
- Calibrated those three support classes by phase so later checkpoints can show when
  board cadence is source-aligned but reward/encounter evidence is still only
  `review_needed` or `hypothesis_draft`.

## Evaluation Autonomous Design Workstream

### Today Goals

- Start the Evaluation & Autonomous Design Model Lab from latest `origin/master`.
- Keep the first slice docs-only and report-only.
- Establish `evaluation_autonomous_design_model_v1` without creating a duplicate V1
  evaluator, hard gate, learned path, default recommender change, or formal card
  generator.

### Completed

- Created `docs/exploration/EVALUATION_AUTONOMOUS_DESIGN_LAB.md`.
- Defined the report-only `evaluation_autonomous_design_model_v1` envelope.
- Added the first entity vocabulary for mechanism candidates, canonical report reads,
  evidence bundles, parameter search spaces, package skeletons, design briefs, and
  rejection reasons.
- Normalized evaluation dimensions across mechanism fun/health, deck compression,
  package health, and design-iteration review language.
- Added a role-only delayed-charge/release example using `charge_turns` as the first
  parameter target.
- Recorded first-slice judgment rules, fallback rejection reasons, and the next round
  entry.
- Added a second docs-only slice for the `charge_turns` parameter probe.
- Wrote a reusable report-only brief template for delayed charge/release that maps
  expected evidence to canonical report-only surfaces.
- Made `fail_state_floor` a required companion parameter for delayed setup probes so
  longer charge windows cannot hide missing recovery value.
- Kept probe wording band-based (`short` versus `medium`) rather than exact balance
  numbers, formal cards, hard gates, or autonomous promotion claims.
- Added a third docs-only slice for the `charge_turns` evidence bundle handoff.
- Documented expected bundle sections, missing-section wording, completeness labels,
  conflict review, and conservative handoff decision wording.
- Included `mechanism_axis_summary` as an optional existing viability read supported by
  the bundle, without making it a new lab-owned report-only surface.
- Added a fourth docs-only slice mapping `charge_turns` rejection reasons to first
  revision actions.
- Added advisory action sequencing so identity swallowing is fixed before payoff
  polishing, reachability claims, or matchup texture.
- Added a compact `revision_note` shape that keeps changes at parameter or package-role
  level and forbids card text, exact balance numbers, hard gates, and learned
  promotion.
- Added a fifth docs-only slice for `charge_turns` package-skeleton readiness.
- Defined prose-only readiness labels for review handoff, payoff-only skeletons,
  goodstuff blur, fail-state gaps, and reachability unknown.
- Added a five-role readiness checklist for anchor, support, glue, payoff, and safety
  valve, with missing-role actions that stay at role level.
- Added ten docs-only follow-up slices for the evaluation/autonomous-design lab.
- Added the first `short` versus `medium` `charge_turns` parameter-search note and
  kept it band-based with `fail_state_floor` as the companion parameter.
- Added foundation-dependency, fail-state-floor, compression-assumption,
  goodstuff/payoff-only, evidence-conflict, and iteration-wording notes for
  `charge_turns`.
- Added enemy-pressure tolerance and package-density balance notes so matchup
  elasticity and support/payoff order remain visible before review-ready wording.
- Added a small `discard_outlet_count` transfer note to verify that the lab vocabulary
  can move beyond `charge_turns` without generating formal cards.
- Promoted the stable lab rules into
  `docs/development/EVALUATION_AUTONOMOUS_DESIGN_MODEL_V1.md`.
- Implemented report-only `evaluation_autonomous_design_model_v1` under
  `tools/combat_analysis/design_engine/` with a CLI, template, snapshots, reports,
  manifests, fixtures, and focused tests.
- Registered the new orchestration surface in the report-only registry using the
  evidence bundle root rather than claiming ownership of canonical summary sections.

## Validation Reference

- Report-only registry:
  - `py -3.11 -m pytest tests/toolkit/combat_analysis/test_report_only_surface_registry_v1.py -q`
  - Result: passed, `5 passed`
  - Slice 2 result: passed, `5 passed`
  - Slice 3 result: passed, `5 passed`
  - Slice 4 result: passed, `5 passed`
  - Slice 5 result: passed, `5 passed`
  - Slice 6-15 batch result: passed, `5 passed`
- Architecture and encoding:
  - `py -3.11 -m pytest tests/toolkit/combat_analysis/test_architecture_boundaries.py tests/shared/test_text_encoding_guards.py -q`
  - Result: passed, `4 passed`
  - Slice 2 result: passed, `4 passed`
  - Slice 3 result: passed, `4 passed`
  - Slice 4 result: passed, `4 passed`
  - Slice 5 result: passed, `4 passed`
  - Slice 6-15 batch result: passed, `4 passed`
- Mechanism-axis discovery contract:
  - `py -3.11 -m pytest tests/toolkit/combat_analysis/test_mechanism_axis_discovery_v1.py tests/scripts/test_run_mechanism_axis_discovery.py -q`
  - Result: passed, `13 passed`
  - Slice 2 result: passed, `13 passed`
  - Slice 3 result: passed, `13 passed`
  - Slice 4 result: passed, `13 passed`
  - Slice 5 result: passed, `13 passed`
  - Slice 6-15 batch result: passed, `13 passed`
- Docs whitespace:
  - `git diff --check`
  - Result: passed, no output
  - Slice 2 result: passed; only Git CRLF working-copy warnings, no whitespace errors
  - Slice 3 result: passed; only Git CRLF working-copy warnings, no whitespace errors
  - Slice 4 result: passed; only Git CRLF working-copy warnings, no whitespace errors
  - Slice 5 result: passed; only Git CRLF working-copy warnings, no whitespace errors
  - Slice 6-15 batch result: passed; only Git CRLF working-copy warnings, no whitespace errors
- Extra text encoding:
  - `py -3.11 -m pytest tests/shared/test_text_encoding_guards.py -q`
  - Slice 6-15 batch result: passed, `3 passed`
- Evaluation autonomous design model:
  - `py -3.11 -m pytest tests/toolkit/combat_analysis/test_evaluation_autonomous_design_model_v1.py tests/scripts/test_run_evaluation_autonomous_design_model.py -q`
  - Implementation result: passed, `16 passed`
- Combined implementation validation:
  - `py -3.11 -m pytest tests/toolkit/combat_analysis/test_evaluation_autonomous_design_model_v1.py tests/scripts/test_run_evaluation_autonomous_design_model.py tests/toolkit/combat_analysis/test_report_only_surface_registry_v1.py tests/toolkit/combat_analysis/test_cardanalysis_evidence_bundle_v1.py tests/toolkit/combat_analysis/test_architecture_boundaries.py tests/shared/test_text_encoding_guards.py tests/toolkit/combat_analysis/test_mechanism_axis_discovery_v1.py tests/scripts/test_run_mechanism_axis_discovery.py -q`
  - Implementation result: passed, `43 passed`
- Final docs whitespace:
  - `git diff --check`
  - Implementation result: passed; only Git CRLF working-copy warnings, no whitespace errors

## Current Blockers

- No hard blocker.
- Phase round bands are initial hypotheses and need later comparison with real
  campaign economy, reward, and encounter pacing.

## Next Step

- Split the calibrated checkpoint evidence more explicitly by block / reward /
  encounter support so each phase label names which source class actually supports
  it.
- Decide where checkpoint-specific `source_support` overrides are needed instead of
  the current phase defaults.
- Run focused validation for the new report-only implementation, fix any contract
  drift, and then prepare main-agent review around phase/checkpoint evidence quality.
- Later compare checkpoint assumptions against actual campaign economy and reward
  pacing before using them for encounter validation.
- Use the new CLI on additional mechanism candidates only as report-only advisory
  evidence.
- If expanding parameter coverage beyond `charge_turns` and `discard_outlet_count`,
  add reviewed-style fixtures and tests before adding new wording.

## Candidate Long-Term Memory

- `campaign_power_curve_model_v1` currently exists as a docs-only exploration surface
  under `docs/exploration/CAMPAIGN_POWER_CURVE_LAB.md`.
- The model is explicitly report-only and must not expose `overall_pass`,
  `hard_gates`, or blocking readiness verdicts.
- The first stable boundary is to express enemy pressure as a vector request before
  any monster numeric implementation exists.
- The first checkpoint suite now covers starter, build, pivot, and mature phases; late
  phase remains intentionally deferred until fail-state and anti-infinite wording is
  more stable.
- Default checkpoint evidence labels are now phase-calibrated: `starter` can be
  `source_aligned` when tutorial loop evidence is present, `early/build/pivot/mature`
  default to `review_needed`, and `late` defaults to `hypothesis_draft`.
- `evidence_refs` alone must not promote a checkpoint to `source_aligned`; payloads
  now carry `calibration_notes` and tighter phase-specific `missing_evidence`.
- Phase round bands are now aligned to the current board's 3-turn slot cadence rather
  than arbitrary per-turn cuts.
- `curve_checkpoint.evidence_state` now separates `block_support`,
  `reward_support`, and `encounter_support`; later phases remain advisory largely
  because those three support classes are uneven.
- `mechanism_online_timing` is phase-aware and report-only; `conditional_online` is an
  honesty label, while `over_online` is a review prompt, not a legality verdict.
- `online_timing_modifiers` separate final-shell coherence from route credibility:
  economy and compression can advance, delay, or condition an online claim, but cannot
  create pass/fail authority.
- Modifier-driven archetypes now exist for build payoff-only detection, pivot
  compression-route probing, and mature over-online texture probing; they are
  validation prompts, not monster stat blocks.
- `enemy_pressure_phase_band` now gives qualitative phase language for pressure
  intensity; it must not be interpreted as damage, health, enemy roster, or
  hard-counter guidance.
- `player_power_reason_code` is now the expected explanation layer for player vector
  scores; scalar-only output should be treated as incomplete report context.
- `deck_maturity_state` now has qualitative labels for assembly shape; labels such as
  `payoff_only` and `overfocused_brittle` are review prompts, not failure verdicts.
- `economy_state` now separates upgrade, removal, shop, event, transform, healing,
  opportunity-cost, and route-risk evidence; missing economy context stays unknown.
- `compression_state` now distinguishes persistent thinning from combat-only
  mitigation; discard filtering and draw selection are not persistent removal.
- `curve_checkpoint` now has a review checklist; checklist status is advisory and
  must not be used as implementation or balance authority.
- Starter/build/pivot/mature payload variants now show phase-specific contract usage;
  they are examples, not schemas or implementation requirements.
- The cardanalysis mapping draft ties curve fields to canonical report-only surfaces;
  it does not add a new CLI, fixture contract, default path, or authority layer.
- `ready_for_main_agent_review`: yes for docs-only exploration review; not ready for
  monster numbers, runtime implementation, hard gates, recommendation default changes,
  or learned/reranker promotion.
- `campaign_power_curve_report_v1` now exists as a canonical report-only surface with
  CLI, fixtures, tests, registry entry, and evidence-bundle support; it remains
  advisory and non-authoritative.
- `evaluation_autonomous_design_model_v1` should be treated as a report-only
  orchestration contract over existing cardanalysis surfaces, not a new evaluator.
- The lab's first role-level package skeleton shape is anchor, support, glue, payoff,
  and safety valve.
- `promote` in this lab means human-review queue only; it does not mean default
  recommendation, hard-gate pass, learned promotion, or formal card creation.
- `charge_turns` probes should be band-based and should always name a companion
  `fail_state_floor`; `none_visible` means revise by wording discipline, not by a new
  hard gate.
- The `charge_turns` evidence bundle handoff should use missing-section wording:
  missing fun/health means `not_evaluated`, missing compression means reachability
  unknown, and missing design iteration means no reject/revise/promote wording yet.
- `charge_turns` revision actions should address identity swallowing before payoff
  ambition; a valid revision note names one parameter or one package role to adjust.
- `charge_turns` package-skeleton readiness should check anchor/support/glue/payoff/
  safety-valve roles before using review-ready wording; payoff-only skeletons stay in
  revision.
- `charge_turns` review should keep parameter bands, foundation dependencies,
  fail-state floors, compression assumptions, and evidence conflicts visible instead
  of collapsing them into a hidden score.
- The lab vocabulary has a first transfer example for `discard_outlet_count`; discard
  must prove whether it is an identity, cost, trigger, resource, or release choice
  rather than generic hand cleanup.
- `evaluation_autonomous_design_model_v1` is now an implemented report-only
  orchestration surface; it consumes existing summaries and outputs advisory wording,
  package readiness, evidence gaps, conflicts, and next report requests only.
