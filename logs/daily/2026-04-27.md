# Daily Log

- Date: `2026-04-27`

## Session: `codex/04-27-asset-file-architecture`

- Branch: `codex/04-27-asset-file-architecture`

## Today Goals

- Turn the recent asset/file-layout discussion into a stable long-term document.
- Clarify the boundary between runtime assets in Git and editable source art in
  an external synced directory.
- Lock the intended semantics of `scratch/` vs `tmp/` and shared-code vs shared-media.

## Completed

- Added `docs/development/PROJECT_FILE_LAYOUT_V1.md` as the target file-layout
  and migration guide for:
  - Unreal-inspired content governance without adopting the full Unreal engine
    object model
  - runtime assets under `assets/`
  - external `PHD_SIMULATER_ART/` as the source-art home
  - manifest-based approved runtime asset mapping
  - `contexts/shared/` as shared code only
  - top-level `tests/`
  - `scratch/` vs `tmp/`
  - optional `skills/` positioning
- Added a small `docs/README.md` discoverability note for the new file-layout rule.

## Current Blockers

- No hard blocker was recorded for this session.

## Next Step

- Decide whether to also record this layout as an accepted decision in
  `docs/pm/DECISION_LOG.md`.
- Add a lightweight local-path convention for the external art root
  (`config/local_paths.local.json` or env var).
- Add manifest and path guardrails once the first migration slice begins.

## Candidate Long-Term Memory

- `docs/development/PROJECT_FILE_LAYOUT_V1.md` is the current source of truth for
  runtime asset placement, external source-art boundaries, top-level test layout,
  and `scratch/` vs `tmp` semantics.

## Current Status

- The repo now has an explicit target architecture for file placement and asset-source separation.
- The document is intentionally written as an end-state plus phased migration
  guide rather than a one-shot rename plan.

## Next Likely Move

1. Decide whether to also record this layout as an accepted decision in
   `docs/pm/DECISION_LOG.md`.
2. Add a lightweight local-path convention for the external art root
   (`config/local_paths.local.json` or env var).
3. Later, add manifest and path guardrails once the first migration slice begins.