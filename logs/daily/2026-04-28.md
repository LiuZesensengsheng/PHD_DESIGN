# Daily Log

- Date: `2026-04-28`
- Active branches:
  - `codex/04-27-gossip-bbs-model-lab`

## Today Goals

- Continue `gossip_bbs_social_simulation_model_v1` as a V1 closeout pass.
- Do not add new event families.
- Keep the work domain-only: no UI, no campaign integration, no persistence
  migration, no real LLM prose generation, and no `cardanalysis` coupling.

## Completed

- Added V1 cross-event invariant tests under
  `tests/gossip_bbs_social_simulation/test_v1_invariants.py`.
- Verified the fixture event surface exactly matches `SUPPORTED_EVENT_TYPES`.
- Verified the fixture transition surface exactly matches
  `SUPPORTED_TRANSITION_TYPES`.
- Added code-level `SUPPORTED_HOOK_TYPES` and `FORBIDDEN_PROSE_KEYS` constants.
- Added invariant coverage for result envelope shape, normalized stance
  distribution, stable hook types, state patch coverage, rumor heat/credibility
  separation, and absence of final prose payload keys.
- Added invariant coverage for key V1 boundaries:
  - `npc_conflict` remains conflict-only before a rumor boundary is crossed.
  - `private_leak` quarantines evidence and blocks credibility transfer.
  - `scandal` keeps rumor truth separate from scandal heat.
  - `faction_event` now emits explicit `player_consent_signal`.
- Updated
  `docs/exploration/GOSSIP_BBS_SOCIAL_SIMULATION_CONTRACT_V1.md`.
- Updated `docs/exploration/GOSSIP_BBS_SOCIAL_SIMULATION_LAB.md` with the V1
  closeout working log.
- Completed the remaining V1 naming/selection micro-pass.
- Added focused tests for:
  - target-rumor topic thread attachment when a thread update omits an explicit
    thread ref;
  - canonical package refs across package/combat/milestone path states;
  - low-salience `boss_defeated` reusing the parent thread without duplicate
    additional-thread patches.
- Tightened V1 invariants so the primary thread cannot also appear in
  `additional_thread_states`.
- Fixed faction-ref-short paths that could otherwise index optional faction refs
  while building relationship deltas.
- Added the Gossip/BBS integration planning pass under
  `docs/exploration/GOSSIP_BBS_SOCIAL_SIMULATION_INTEGRATION_PLAN_V1.md`.
- Selected a sidecar reaction pipeline as the recommended future shape:
  reviewed campaign/combat/narrative facts -> campaign-owned adapter -> runtime
  service -> sidecar state transitions -> copy-free read model.
- Recorded integration stop lines: no direct UI ownership, no forced-event
  runtime ownership, no save-schema mutation, no LLM prose, no database, and no
  `cardanalysis` coupling.
- Added the first adapter contract slice:
  `contexts/campaign/services/gossip_bbs_event_adapter.py`.
- Added focused adapter coverage:
  `tests/campaign/test_gossip_bbs_event_adapter.py`.
- Mapped one reviewed `AFTER_COMBAT_RETURN` fact to one `combat_result`
  `GossipEventInput`, with no runtime service or lifecycle hook wiring.
- Added a missing-public-evidence negative path that emits no sidecar event and
  returns a warning.
- Added an import-boundary test so the adapter stays mapping-only and does not
  import sidecar transitions, UI, pygame, or `cardanalysis`.

## Validation Reference

- Required:
  - `py -3.11 -m pytest tests/gossip_bbs_social_simulation -q`
  - `py -3.11 -m pytest tests/shared/test_text_encoding_guards.py -q`
  - `git diff --check`

## Current Blockers

- No hard blocker.

## Next Step

- V1 sidecar remaining closeout tasks are complete for the current defined
  scope.
- Future work should start from a new explicit goal:
  - runtime-only `GossipBbsRuntimeService` contract,
  - additional reviewed event fixtures,
  - or tuning/evaluation.
- Do not open UI, campaign persistence, database work, real BBS prose, LLM
  generation, or `cardanalysis` coupling without a separate integration
  decision.
- If integration continues next, apply already-created adapter outputs to an
  in-memory `GossipSimulationState`; do not wire lifecycle hooks or write save
  data in that slice.

## Candidate Long-Term Memory

- Gossip/BBS V1 now has a full supported upstream event surface plus cross-event
  structural invariants.
- `SUPPORTED_HOOK_TYPES` is the stable code-level hook surface for the sidecar.
- `FORBIDDEN_PROSE_KEYS` guards against storing final forum copy in domain
  payloads.
- `faction_state.player_consent_signal` is explicit so faction claims do not
  overwrite actor agency by implication.
- Thread selection now prefers explicit thread/topic refs, then target-rumor
  topic matches, then the most recent known thread at or before the event turn.
- Package path states now emit canonical `package_<slug>` refs across the V1
  sidecar.
- Future integration should use a campaign-owned adapter/service pair while the
  sidecar remains campaign-import-free.
- The first campaign-owned adapter slice maps public combat-return facts into
  sidecar inputs only; it does not execute sidecar transitions.
