# Daily Log

- Date: `2026-04-28`
- Active branches:
  - `codex/04-27-power-curve-model-lab`
  - `codex/04-27-gossip-bbs-model-lab`
  - `codex/04-28-power-curve-report-v1-integration`

## Today Goals

- Close and integrate report-only/model-lab work into a single mainline-friendly
  branch without semantic drift.
- Carry `position_redirect_threat_control` from reviewed preflight into a bounded
  runtime/data contract, not a full content or family-promotion push.
- Advance gossip/BBS from standalone sidecar model toward a campaign-owned
  `AFTER_COMBAT_RETURN` owner seam without wiring real lifecycle hooks yet.

## Completed

- Power Curve Model Lab, rounds 20-24:
  - Added checkpoint-specific `source_support` overrides for reviewed build and pivot
    cases.
  - Added `curve_checkpoint.anti_infinite_boundary` as report-only recovery texture,
    not loop legality or hard-gate authority.
  - Added `encounter_archetype_profile`, including status-heavy and multi-enemy
    archetype probes that stay in advisory-context territory.
  - Backfilled archetype profiles across the reviewed fixture suite and closed the V1
    report-only review surface.
- Mainline report-only consolidation:
  - Preserved `evaluation_autonomous_design_model_v1` and
    `campaign_power_curve_report_v1` as current canonical surfaces.
  - Integrated `position_redirect_code_preflight_v1` into the report-only registry,
    evidence bundle, mechanism-discovery doc, and default entrypoints.
  - Deliberately did not merge the broader `mechanism_axis_probe_review_v1` surface
    into this branch; this round only needed the position-redirect preflight handoff
    input.
- Position redirect preflight handoff:
  - Ran `scripts/run_position_redirect_code_preflight.py` against
    `position_redirect_code_preflight_reviewed_v1.json`.
  - Confirmed the reviewed conclusion stays
    `code_handoff_ready_with_bounds` / `go_with_bounds`.
  - Kept required bounds explicit: one or two displacement vectors, compact readable
    telegraph preview, no full-solution overlay, fail-state partial-solve logging, and
    damage/defense remaining comparison axes rather than identity.
- Bounded runtime/data prototype slice:
  - Added `contexts/combat/domain/position_redirect_preview.py` as a default-off
    combat-domain contract for compact position-redirect telegraph preview.
  - Added `position_redirect_preview` as an optional `Enemy` intent-summary payload so
    later UI/runtime work can consume it without changing current gameplay flow.
- Gossip/BBS closeout and campaign seam:
  - Added integration planning docs for a campaign-owned adapter/service seam.
  - Added `contexts/campaign/services/gossip_bbs_event_adapter.py`, mapping reviewed
    combat-return facts into `GossipEventInput`.
  - Added `contexts/campaign/services/gossip_bbs_runtime_service.py`, applying
    reviewed gossip events to in-memory `GossipSimulationState` and accumulating
    non-blocking warnings.
  - Added `contexts/campaign/services/gossip_bbs_after_combat_return_coordinator.py`
    as a duplicate-guard coordinator for `AFTER_COMBAT_RETURN` facts.
  - Kept the integrated chain at:
    `reviewed AFTER_COMBAT_RETURN fact -> adapter -> runtime service ->
    duplicate-guard coordinator -> in-memory GossipSimulationState`.
  - Mounted the first explicit campaign owner seam:
    `CampaignLifecycleBindingService` now registers
    `system:gossip_bbs_after_combat_return` on `AFTER_COMBAT_RETURN`, passing one
    `CampaignLifecycleContext.reviewed_after_combat_return_fact` into the grouped
    coordinator when a reviewed fact is supplied.

## Validation

- Position redirect:
  - `python scripts/run_position_redirect_code_preflight.py --input tests/fixtures/combat_analysis/position_redirect_code_preflight_v1/position_redirect_code_preflight_reviewed_v1.json --output-dir tmp/combat_analysis/position_redirect_code_preflight_v1`
  - `py -3.11 -m pytest tests/combat/test_position_redirect_preview_contracts.py tests/toolkit/combat_analysis/test_position_redirect_code_preflight_v1.py tests/scripts/test_run_position_redirect_code_preflight.py -q`
- Report-only integration:
  - `py -3.11 -m pytest tests/toolkit/combat_analysis/test_cardanalysis_evidence_bundle_v1.py tests/toolkit/combat_analysis/test_report_only_surface_registry_v1.py -q`
- Gossip/BBS:
  - `py -3.11 -m pytest tests/gossip_bbs_social_simulation -q`
  - `py -3.11 -m pytest tests/campaign/test_gossip_bbs_event_adapter.py tests/campaign/test_gossip_bbs_runtime_service.py tests/campaign/test_gossip_bbs_after_combat_return_coordinator.py -q`
  - `py -3.11 -m pytest tests/campaign/test_campaign_lifecycle_binding_service.py tests/campaign/test_campaign_return_resolution_flow.py -q`
- Shared guards:
  - `py -3.11 -m pytest tests/combat/test_combat_core_state_contracts.py tests/toolkit/combat_analysis/test_architecture_boundaries.py tests/shared/test_text_encoding_guards.py -q`
  - `python scripts/validate_architecture.py`
  - `git diff --check`

## Current Blockers

- No hard blocker.
- Position redirect remains default-off and is not wired into enemy content loading,
  combat rendering, or synthesis.
- Gossip/BBS now has one real `AFTER_COMBAT_RETURN` lifecycle owner seam, but it
  still has no save/persistence, UI, database, LLM prose, cardanalysis coupling,
  or additional lifecycle-window wiring.

## Next Step

- Review the new `AFTER_COMBAT_RETURN` owner seam against real combat-return
  producers and decide where the reviewed fact should be created.
- Do not expand gossip/BBS into other lifecycle windows until this first owner seam is
  reviewed.

## Candidate Long-Term Memory

- `position_redirect_code_preflight_v1` says `go_with_bounds`, not formal family
  promotion.
- `position_redirect_preview.py` is the bounded runtime/data contract for future
  compact telegraph UI work.
- Gossip/BBS V1 now has a sidecar model, campaign-owned adapter/runtime service,
  duplicate-guard coordinator, and one `AFTER_COMBAT_RETURN` lifecycle owner seam;
  it still has no persistent save or UI layer.
