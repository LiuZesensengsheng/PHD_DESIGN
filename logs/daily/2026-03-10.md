# Daily Log

- Date: `2026-03-10`
- Branch: `codex/03-10-next-step`
- Key commit:
  - `8037e72` Restore red white card descriptions and add text guardrails

## Today Goals

- Investigate why some combat card detail text became garbled again
- Confirm whether the issue was in rendering, runtime loading, or source data
- Repair the current red/white card text without losing recent balance changes

## Completed

- Traced the combat detail text path and confirmed runtime reads card text from generated JSON, not directly from CSV
- Verified the rendering layer was not the primary issue; the corruption was already present in card data
- Confirmed the active corruption lived in:
  - `data/cards/white/cards_white.csv`
  - `data/cards/red/cards_red.csv`
  - and the corresponding `cards_generated.json`
- Compared current CSV against historical commits and identified `9b385bd` as the last known-good source for the damaged red/white text rows
- Restored only the corrupted `description_key` values from the good historical CSV while preserving later balance-number changes
- Regenerated red/white `cards_generated.json`
- Added generation-time guardrails so card text generation now fails if `name_key` or `description_key` contains suspicious placeholder characters such as `?` or `�`
- Added tests covering:
  - failure on missing display text
  - failure on suspicious placeholder text
  - current red/white CSV text integrity
- Ran targeted tests and full `python -m pytest -q`, all green
- Aligned the default Codex branch naming convention to `codex/MM-DD-topic` and renamed the active branch accordingly

## Root Cause

- The garbled combat detail text was caused by corrupted `description_key` values in the red/white CSV source data
- Runtime was working as designed:
  - CSV is the editing source
  - `cards_generated.json` is the runtime canonical source
- A later balance pass modified red/white CSV rows and some description text was written back as `????`
- Older bootstrap JSON files under `data/cards/red/` and `data/cards/white/` are not reliable recovery sources because some of them already contain older mojibake-style corruption

## Current Blockers

- The exact tool/editor workflow that converted some CSV text into `????` is still not fully identified
- Old bootstrap JSON card files under `data/cards/red/` and `data/cards/white/` still contain historical mojibake and should not be treated as trusted text sources

## Next Step

- If this happens again, compare the active CSV against the last known-good commit before touching rendering code
- Consider adding a small documented workflow for card-balance edits:
  - edit CSV in UTF-8-safe tooling
  - regenerate JSON immediately
  - run card generation tests before commit
- Later, decide whether old bootstrap JSON files should be normalized or explicitly deprecated in docs

## Candidate Long-Term Memory

- Card data workflow rule: CSV is the editable source, `cards_generated.json` is the runtime canonical artifact
- Guardrail rule: card generation must fail on suspicious placeholder characters in display text
- Recovery rule: if card detail text appears garbled in combat, inspect CSV and generated JSON before inspecting rendering code
- Workflow rule: default Codex branch naming should use `codex/MM-DD-topic`
