# Daily Log

- Date: `2026-03-31`
- Branch: `codex/03-31-non-visual-taskboard`

## Today Goals

- Start non-visual `Phase 1` for combat runtime cleanup
- Reduce one real queue-external fallback seam without touching the visual/editor refactor branch
- Complete `Phase 2A` by deduplicating the per-effect runtime skeleton shared by card play and top-of-draw
- Keep combat and simulation regressions green after the change

## Completed

- Re-checked the combat runtime inventory and narrowed `Phase 1` to a bounded seam:
  - `PlayTopOfDrawThenExhaustEffect.execute(...)` in [pile.py](/D:/PHD_SIMULATER/contexts/combat/domain/effects/impl/pile.py)
- Replaced the local top-card effect loop with orchestration reuse:
  - direct fallback execution now builds a local `EffectExecutor`
  - then delegates into `ActionExecutor` via `PlayTopOfDrawThenExhaustAction`
  - this keeps existing dependency injection behavior while reusing the queue-native top-of-draw path
- Added regression coverage in [test_action_queue.py](/D:/PHD_SIMULATER/tests/combat/test_action_queue.py):
  - verifies `EffectExecutor.execute_effect_once(...)` for `play_top_of_draw_then_exhaust` still works even when `state.player.card_repo` is absent
  - ensures the top card resolves, the next card is drawn, and the top card is exhausted
- Removed the last active planner-negative card effect in the current white/red pool:
  - `swap_with_paper` now has planner support plus a queue action/executor path
  - active fallback scan in [test_active_queue_boundaries.py](/D:/PHD_SIMULATER/tests/combat/test_active_queue_boundaries.py) now expects an empty set
- Added regression coverage for the paper ally swap path:
  - planner unit coverage in [test_effect_planner.py](/D:/PHD_SIMULATER/tests/combat/test_effect_planner.py)
  - action execution coverage in [test_action_queue.py](/D:/PHD_SIMULATER/tests/combat/test_action_queue.py)
  - card-play integration coverage in [test_effect_planner.py](/D:/PHD_SIMULATER/tests/combat/test_effect_planner.py)
- Updated runtime inventory in [COMBAT_FALLBACK_AND_SHIM_INVENTORY_V1.md](/D:/PHD_SIMULATER/docs/development/COMBAT_FALLBACK_AND_SHIM_INVENTORY_V1.md):
  - marked the old `PlayTopOfDrawThenExhaustEffect` inline loop seam as resolved
  - recorded `swap_with_paper` as planner-native for the active card pool
- Completed non-visual `Phase 2A` by deduplicating the shared per-effect resolution skeleton:
  - added [effect_resolution_runner.py](/D:/PHD_SIMULATER/contexts/combat/application/orchestration/effect_resolution_runner.py)
  - `CardPlayOrchestrator._resolve_effects_one_by_one(...)` now delegates planner/fallback/follow-up sequencing into the shared runner
  - `ActionExecutor._resolve_top_card_effects(...)` now uses the same runner, so top-of-draw follows the same planner/fallback drain order as normal card play
- Added regression coverage for the shared top-card path in [test_action_queue.py](/D:/PHD_SIMULATER/tests/combat/test_action_queue.py):
  - verifies planner-negative top-card effects still hit `execute_effect_once(...)`
  - verifies fallback follow-up actions are drained through the shared runtime path
- Continued into `Phase 2B` with a minimal runtime base for the remaining design-v2 storage effects:
  - added `CombatState` carriers for loaded steadfast tokens and registered paradigms
  - added factory/registry support plus fallback implementations for `add_steadfast_token` and `add_paradigm`
  - added planner/action mainline support so both effect families now resolve without falling back to unknown-type failures
- Added regression coverage for the new storage effect families:
  - planner coverage in [test_effect_planner.py](/D:/PHD_SIMULATER/tests/combat/test_effect_planner.py)
  - action execution coverage in [test_action_queue.py](/D:/PHD_SIMULATER/tests/combat/test_action_queue.py)
  - integration coverage for actual card play into `CombatState.paradigms` and `CombatState.steadfast_tokens`
- Ran a repository-level content scan over `data/cards`:
  - effect-type recognition by planner/mainline is now at zero unknown types for the checked-in card JSONs

## Validation

- `py -3.11 -m pytest tests/combat/test_action_queue.py -q`
- `py -3.11 -m pytest tests/combat/test_active_queue_boundaries.py -q`
- `py -3.11 -m pytest tests/combat/test_effect_planner.py -q`
- `py -3.11 -m pytest tests/combat/test_card_play_orchestrator.py -q`
- `py -3.11 -m pytest tests/combat -q`
- `py -3.11 -m pytest tests/simulation -q`
- `py -3.11 -m pytest -q`

## Current Status

- `play_top_of_draw_then_exhaust` no longer maintains its own local effect loop in the domain effect implementation.
- `swap_with_paper` is now queue-native, which clears the active white/red fallback scan to zero.
- `Phase 2A` is now complete: the duplicated per-effect planner/fallback/follow-up skeleton has been centralized in `effect_resolution_runner.py`.
- The remaining runtime question is no longer duplicate orchestration code; it is which planner-negative effect families should still fall through `execute_effect_once(...)` and which should become queue-native next.
- The current non-visual package now has three concrete outcomes: top-of-draw effect looping removed from the domain layer, `swap_with_paper` queued, and card-play/top-of-draw effect sequencing unified under one runtime spine.
- `Phase 2B` now adds a minimal storage-layer base for `add_steadfast_token` and `add_paradigm`: both can build, plan, execute, and persist state through the mainline.
- Repository card JSONs no longer contain effect types that are unknown to planner/mainline by type.
- Important boundary: `white_design_v2` is still not part of the active runtime pack load path, and full trigger semantics for paradigm/steadfast remain future work.

## Next Step

1. Decide whether the next non-visual slice is:
   - activating `white_design_v2` through the runtime content load path
   - or implementing the first real trigger semantics for loaded steadfast tokens / registered paradigms
2. If we stay on runtime semantics first, start from one narrow trigger:
   - a single steadfast consume rule
   - or a single paradigm activation window
