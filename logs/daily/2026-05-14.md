# Daily Log

- Date: `2026-05-14`
- Active branches:
  - `codex/05-14-test-speed-risk-review`
  - `codex/05-14-content-pack-resource-inventory`
  - `codex/05-14-pack-aware-data-pipeline-guard`

## Today Goals

- Rebase test-speed follow-up work onto the latest `master`.
- Review higher-risk remaining speed opportunities without weakening the full
  pytest regression gate.
- Continue option B by adding the first post-registry content-pack slice.
- Build a report-only resource inventory over active source packs before any
  resolver or runtime loader changes.
- Add the first pack-aware data pipeline guard that consumes the registry and
  inventory read models without changing runtime loading.
- Record the near-term cardanalysis pivot toward control-only card-package
  production trials.
- Keep the control production trial report-only and separate from official
  runtime card data.
- Create a clear owner-facing target for producing several concrete control
  package versions before expanding more blocked downstream scaffolds.
- Add a small report-only STS1 exam capability calibration slice that does not
  touch runtime card data, formal card text, scorecard weights, or the control
  card-production packet line.
- Add the first human-facing report-only control card production packet after
  the production trial plan merges.

## Completed

- Confirmed `codex/05-14-test-speed-risk-review`, `origin/master`, and fetched
  `FETCH_HEAD` all pointed at the same latest-master commit during the
  test-speed branch.
- Repaired a Windows long-path blocker in the
  `control_discipline_ideal_draft_submission_readiness_v1` fixture by renaming
  only artifact filenames and updating artifact path references. Payload IDs
  remain unchanged.
- Consolidated duplicate assertions in
  `tests/shared/test_headless_repro_runner.py`: the deterministic repro test now
  also asserts built-in case success, final state, and snapshot shape, removing
  one extra success-only run per built-in headless repro case.
- Tested but rejected cross-file session caching for headless repro results.
  Full-suite order exposed deterministic snapshot drift for `combat_reward_loop`
  and `thesis_judgment_to_combat`, so the cache was removed.
- Added `ContentPackInventory` as a shared read model over discovered source
  packs.
- Added `scripts/content_pack_inventory.py` to print Markdown or JSON inventory
  reports and optionally write both formats.
- The inventory records pack identity, source files, declared runtime outputs,
  missing runtime outputs, and missing dependency ids.
- Current inventory makes the active split explicit:
  - `tutorial` is a narrative source pack with three present runtime outputs;
  - `slack` is an event source pack with no declared runtime outputs yet.
- Updated content-pack docs with the inventory read model and command runbook.
- Rebased `codex/05-14-pack-aware-data-pipeline-guard` onto latest
  `origin/master` at `d5143041` after PR #172.
- Updated active data pipeline contracts to discover source packs through
  `ContentPackRegistry` and validate declared runtime-output state through
  `ContentPackInventory`.
- The tutorial narrative source-pack guard now checks the three declared runtime
  outputs through inventory and runs all-pack narrative source parity via
  `build_narrative_runtime.py --all --check`.
- The slack event source pack remains an explicit report-only pack with no
  declared runtime outputs.
- Added Control Card Production Trial Plan V1.
- Defined the first trial as three to five report-only control package versions
  with fun, strength, strength-curve, combo, risk, and evidence notes.
- Preserved the owner-unavailable boundary: automation may prepare and compare
  packets, but official card text, runtime data, promotion, and playable
  prototype implementation remain blocked until human review.
- Added sts1_exam_capability_calibration_v1, a deterministic sidecar over
  existing STS1 happy-path drafts and generated-attempt boundary controls.
- The calibration names two missing explanation dimensions:
  primary_axis_dominance and early_floor_late_ceiling_balance.
- Added CLI, fixture artifacts, focused toolkit/script tests, default entrypoint,
  capability graph registration, and the V1 development doc.
- Added control_card_production_packet_v1, the first owner-facing control-only
  production packet from the trial plan.
- The packet produces five report-only package versions:
  white_order_fail_state_packet_v1, blue_truth_forecast_packet_v1,
  green_depth_compound_stability_packet_v1, resource_tempo_denial_probe_v1, and
  hand_future_planning_probe_v1.
- Each version includes a play promise, intended loop, role slots, eight
  semi-formal concept slots, fun/strength/curve notes, combo and anti-combo
  notes, risk notes, evidence/review questions, blocked fields, and recommended
  next action.
- Added sts1_negative_case_pack_v1, a deep first-round STS1 negative and
  boundary case pack with 16 normalized unreviewed cases, four per STS1
  character.
- The case pack covers primary-axis drift, secondary-axis swallowing,
  generic-goodstuff drift, early weak / late explosive curve risk, numeric
  fantasy, weak fail states, character-texture mismatch, and anti-combo role
  collision without starting the control-candidate case line.

## Boundary Notes

- Report-only content-pack slice only.
- No runtime resolver, runtime activation, dependency solver, hot reload, save
  pack pinning, UI change, visual asset migration, or content balance change.
- `cardanalysis` / `combat_analysis` implementation stays out of scope for the
  inventory slice; the merged `origin/master` fixture rename is from the
  test-speed branch.
- The pack-aware data pipeline guard only tightens tests/docs around
  registry/inventory consumption. It does not add a runtime resolver, change
  save schema, introduce hot reload, or modify content balance.

## Risk Notes

- The kept headless change is bounded to one test file and still runs each
  built-in repro case twice inside the deterministic test.
- `tests/shared/test_e2e_headless_encounter_smoke.py` remains a dedicated smoke
  entrypoint because smoke scripts and docs rely on that path.
- Modelization shadow CLI remains the largest single cold test. Snapshot-input
  or synthetic-summary writer tests could reduce one test but would change the
  CLI coverage boundary, so this was not changed in the test-speed pass.
- `project_design_assist_session_batch` remains a real campaign-summary
  integration suite. Snapshot-only summary tests may be possible later, but that
  would need a separate coverage decision.
- Content-pack inventory is an audit/resolver input surface, not runtime
  activation.
- The control production trial is planning and report-only output. It does not
  write runtime card data or modify official cards.
- Semi-formal card concept slots are allowed as review material, but they are
  not official card text.
- Existing exam, diagnostic, and evidence layers remain advisory context and do
  not become hard gates or authoritative promotion decisions.
- The STS1 calibration is advisory context only. It does not generate formal
  cards, write runtime card data, change package exam outcomes, change
  scorecard dimensions or weights, create hard gates, call an LLM/API, or claim
  reviewed evidence.
- The STS1 negative case pack is generated hypothesis material only. It does
  not contain formal card text, does not write runtime card data, does not
  change scorecard weights or exam outcomes, and does not claim reviewed
  evidence.
- Human review still owns fun judgment, archetype identity, numeric bands,
  formal wording, evidence promotion, and runtime promotion/rejection.
- The control card production packet is advisory and human-facing only. It
  creates semi-formal concept slots for review, not official card text, runtime
  card data, promotion decisions, hard gates, learned behavior, or LLM/API
  calls.
- The packet stays `review_status = awaiting_owner_review` and
  `blocked_by_owner_review = true` until the owner selects, revises, rejects, or
  merges package versions.

## Current Blockers

- No known implementation blocker for the merged content-pack inventory line.
- Human review is still required before any selected control package can become
  formal card text, runtime card data, or a playable prototype target.
- The next control production implementation slice needs either an owner input
  packet or explicit permission to use the current control discipline plus ideal
  pilot as provisional context.
- No implementation blocker for the STS1 calibration slice.
- The STS1 calibration remains a readout; any future scorecard dimension or
  weight change needs a separate explicit PR.
- No implementation blocker for the V1 control card production packet. Human
  review is still required before any package version becomes a complete-card
  draft request or playable prototype target.
- No implementation blocker for the first STS1 negative case pack. Later
  promotion from unreviewed case material to reviewed evidence still requires
  explicit human review.

## Validation

- Test-speed branch validation:
  - `py -3.11 -m pytest tests/shared/test_headless_repro_runner.py tests/shared/test_e2e_headless_encounter_smoke.py -q --durations=20 --durations-min=0.05`
  - `py -3.11 -m pytest tests/toolkit/combat_analysis/test_control_discipline_ideal_draft_submission_readiness_v1.py tests/scripts/test_run_control_discipline_ideal_draft_submission_readiness.py -q`
  - `py -3.11 -m pytest tests/shared/test_text_encoding_guards.py -q`
  - `git diff --check`
  - `py -3.11 -m pytest -q --durations=120 --durations-min=0.05`
- Content-pack inventory branch validation:
  - `py -3.11 -m pytest tests/shared/test_content_pack_inventory.py tests/shared/test_content_pack_registry.py tests/scripts/test_content_pack_manifest.py -q`
  - `py -3.11 -m pytest tests/shared/test_content_pack_inventory.py tests/shared/test_content_pack_registry.py tests/scripts/test_content_pack_manifest.py tests/scripts/test_data_pipeline_contracts.py -q`
  - `python scripts/content_pack_inventory.py --json`
  - `python scripts/content_pack_inventory.py`
  - `python scripts/content_pack_manifest.py --discover-source-packs --require-complete-dependencies`
  - `python scripts/check_resource_contracts.py`
  - `py -3.11 scripts/run_test_smoke.py --profile quick`
  - `py -3.11 scripts/run_test_smoke.py --profile contract`
  - `python scripts/check_text_encoding.py`
  - `git diff --check`
  - `python scripts/check_project_memory_health.py --today 2026-05-14`
  - `py -3.11 -m pytest -q` from `C:\p\i`
- Control card production packet branch validation:
  - `python scripts/run_control_card_production_packet.py --output-dir tests/fixtures/combat_analysis/control_card_production_packet_v1`
  - `python scripts/run_control_card_production_packet.py --input tests/fixtures/combat_analysis/control_card_production_packet_v1 --json`
  - `py -3.11 -m pytest tests/toolkit/combat_analysis/test_control_card_production_packet_v1.py tests/scripts/test_run_control_card_production_packet.py tests/toolkit/combat_analysis/test_capability_graph_registry_v1.py -q`
  - `python scripts/validate_capability_graph.py`
  - `python scripts/check_project_memory_health.py`
  - `python scripts/check_text_encoding.py`
  - `git diff --check`
- STS1 negative case pack focused validation to run before merge:
  - `py -3.11 -m pytest tests/toolkit/combat_analysis/test_sts1_negative_case_pack_v1.py tests/scripts/test_run_sts1_negative_case_pack.py -q`
- Content-pack data pipeline guard branch validation:
  - `python scripts/content_pack_manifest.py --discover-source-packs --require-complete-dependencies`
  - `python scripts/content_pack_inventory.py --json`
  - `python scripts/build_narrative_runtime.py --all --pack-root data/narrative_src/packs --check`
  - `py -3.11 -m pytest tests/scripts/test_data_pipeline_contracts.py -q`
  - `py -3.11 -m pytest tests/shared/test_content_pack_inventory.py tests/shared/test_content_pack_registry.py tests/scripts/test_content_pack_manifest.py tests/scripts/test_data_pipeline_contracts.py tests/scripts/test_narrative_draft_pipeline.py -q`
  - `python scripts/check_text_encoding.py`
  - `git diff --check`
  - `python scripts/check_project_memory_health.py --today 2026-05-14`
  - `py -3.11 scripts/run_test_smoke.py --profile quick`
  - `py -3.11 scripts/run_test_smoke.py --profile contract`
  - `py -3.11 -m pytest -q`

## Next Step

- Commit the pack-aware data pipeline guard and open the focused PR.
- Recommended next implementation slice after this guard merges:
  introduce the smallest source-pack runtime-output read model or resolver
  input, still without runtime activation, hot reload, save pinning, or
  dependency solving.
- If more test-speed work is needed, evaluate modelization shadow boundaries as
  an explicit decision rather than a quick test shortcut.
- After the control production trial plan merges, build the human-facing control
  package production packet before adding more blocked readiness scaffolds.
- After the STS1 calibration merges, keep exam explanation work sidecar and
  report-only unless a later scorecard-owner PR explicitly changes dimensions
  or weights.
- After the control card production packet merges, ask the owner to select one
  or two package versions. If the owner is available, continue into a
  complete-card draft request packet; if not, improve comparison/repair notes
  without drafting formal cards.
- For the case-expansion line, run focused case-pack tests, case-input
  validation, capability graph validation, memory health, encoding, diff, and
  full pytest before opening the PR.

## Candidate Long-Term Memory

- Content-pack resource work is proceeding as registry/read-model first,
  inventory second, then resolver. The inventory is an audit/resolver input
  surface, not runtime activation.
- Active data pipeline guards should consume the content-pack registry and
  inventory before any future resolver/runtime-loader work.
- Headless repro flows should not share cached runtime results across test files
  without a deeper isolation design; a full-suite trial showed deterministic
  snapshot drift for `combat_reward_loop` and `thesis_judgment_to_combat`.
- The dedicated headless smoke file remains part of the smoke profile contract,
  so optimize duplicate repro execution inside adjacent tests before weakening
  that smoke entrypoint.
- Near-term cardanalysis focus should shift from additional blocked downstream
  scaffolds to a control-only production trial that creates three to five
  report-only package versions for human review.
- Control package production remains advisory until a human selects package
  versions for draft requests or playable prototype work.
- STS1 exam explanation work should stay sidecar/report-only unless a later
  scorecard-owner PR explicitly changes scorecard dimensions or weights.
- The current first calibration dimensions are primary_axis_dominance and
  early_floor_late_ceiling_balance.
- Control card production packet V1 is the first concrete control-card design
  output: five unreviewed package versions are readable by the owner, but still
  blocked from draft/runtime promotion until owner review.
- First-round STS1 case expansion now means deepening the unreviewed normalized
  case pack, not generating more formal card drafts and not starting the
  control-candidate case pack line.
