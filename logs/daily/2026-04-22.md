# Daily Log

- Date: `2026-04-22`
- Compression status: compressed on `2026-04-26`
- Weekly bridge: `docs/logs/weekly/2026-W17.md`

## Today Goals

- Expand external `STS1` reviewed holdout coverage without using project-local red-card
  progress as the main autonomy signal.
- Continue campaign direct-service cleanup toward grouped ownership and an explicit
  trigger surface.
- Continue combat session-first migration and reduce low-value `CombatModel` facade
  pressure.

## Completed

### Combat Analysis Holdout Expansion

- Added `s35_ironclad_holdout_shockwave_v1` to the reviewed external STS holdout
  surface and kept the benchmark green.
- Ran honest probe sweeps before adding more fixtures:
  - several Ironclad follow-ups remained too semantically noisy
  - Silent `dash_vs_nob` became the first clean enough Silent starter case
  - Watcher `wallop_vs_book` became a clean enough starter case
  - Defect `cold_snap` / frost-control repair contexts became promising enough to land
    starter coverage
- Clarified that `sts1` was the only family ready for active reviewed holdout expansion;
  `sts2` was present as source material but not yet equally benchmark-ready.
- By the end of the line, reviewed external STS coverage spanned all four modeled
  `sts1` characters.

### Campaign Grouped Services And Trigger Surface

- Removed direct `CampaignState` aliases for grouped thesis/social service families.
- Removed additional interaction aliases for lifecycle binding, block-click,
  event-input, turn, and end-turn orchestration.
- Preserved state-level shell/request seams such as thesis submission, reward open,
  event request, end-turn request, and campaign turn advance.
- Added `Task Area Trigger Surface V1`:
  - trigger record model
  - grouped trigger collector
  - state-level record/peek/consume/clear/snapshot/restore seams
  - lifecycle windows and task-area domain events feeding one explicit reactive surface
  - rollback restores trigger-buffer state with board/domain state
- Added the direct-seam policy note for remaining campaign direct aliases.

### Combat Session-First Migration

- Added explicit render-facing session reads:
  - `current_phase`
  - `phase_banner`
  - `enemy_action_banner`
- Moved render projection off private session compatibility fields.
- Detached headed production chain from eager `CombatModel` ownership:
  - scene builder and controller moved toward `CombatSession`
  - removed headed/headless model fallback surfaces and tuple conversions
  - helper/test fixtures became session-first
- Tightened `CombatModel.__init__` to keyword-only `session` usage and migrated many
  runtime-semantics tests to direct `CombatSession`.

## Current Status

- External STS holdout work was moving from "add missing starter packs" toward
  "increase honest per-character breadth and reduce semantic drift".
- Campaign service ownership was substantially clearer:
  grouped thesis/social/reward leaf services, shell/runtime seams on `CampaignState`,
  and a single trigger queue for future trait/event/forced-event consumers.
- Combat headed/headless runtime setup was increasingly session-first, with remaining
  facade pressure concentrated in presentation/controller/content coverage.

## Validation Reference

- Combat analysis:
  - direct benchmark runs for reviewed STS holdout packs
  - focused holdout benchmark tests where the local interpreter supported pytest
  - text encoding guard checks
- Campaign:
  - focused grouped-service, dependency narrowing, trigger-surface, lifecycle, and
    guardrail packs
  - `py -3.11 -m pytest tests/campaign -q`
- Combat:
  - focused runtime host migration, render-state contract, controller, simulation,
    timing, and session-first helper packs
  - known red-card cluster recorded as unrelated to constructor migration

## Next Step

- Continue reviewed STS expansion only where local outputs stay semantically honest.
- Use the campaign trigger surface for future reactive consumers instead of raw
  lifecycle internals or direct `state.domain_events` peeking.
- Continue combat facade shrinkage only where test/runtime ownership becomes clearer.

## Candidate Long-Term Memory

- Reviewed external benchmark expansion should be evidence-led; formal pass metrics are
  not enough when recovered shapes are semantically wrong.
- Campaign direct aliases should be either shell/runtime ports or grouped leaf-service
  ownership, not duplicated convenience paths.
- Combat migration should prefer `CombatSession` and named collaborators over facade or
  tuple compatibility paths.

## Promotion Notes

- Combat-analysis autonomy posture is promoted through:
  - `tools/combat_analysis/docs/MODELIZATION_MIGRATION_PATH_V1.md`
  - `tools/combat_analysis/docs/AUTONOMY_READINESS_SCORECARD_V1.md`
- Campaign trigger/direct-seam facts are promoted through:
  - `docs/development/CAMPAIGN_TASK_AREA_TRIGGER_SURFACE_V1.md`
  - `docs/development/CAMPAIGN_DIRECT_SEAM_POLICY_V1.md`
- Combat session-first cleanup facts are promoted through:
  - `docs/development/COMBAT_RUNTIME_SURFACE_INVENTORY_V1.md`
  - `docs/development/COMBAT_GLOBAL_COMPAT_ZERO_AUTOMATION_V1.md`

## Compression Notes

- This file intentionally replaces detailed per-command history with a handoff summary.
- The original long-form execution log is recoverable from git history before the
  compression commit.
