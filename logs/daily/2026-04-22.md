# Daily Log

- Date: `2026-04-22`

## Session: `codex/04-18-cardanalysis-dataset-expansion`

- Branch: `codex/04-18-cardanalysis-dataset-expansion`

## Today Goals

- Expand the reviewed external STS catalog holdout benchmark without falling back to
  project-local `red` signals.
- Validate the first post-starter-pack Ironclad addition honestly against real local
  outputs.
- Reconfirm which catalog families are actually ready for reviewed holdout expansion.

## Completed

- Re-read the active project continuity files before touching the benchmark lane:
  - `AGENTS.md`
  - `docs/development/CURRENT_DIRECTION.md`
  - `docs/development/DEFAULT_ENTRYPOINTS.md`
  - `docs/pm/DECISION_LOG.md`
  - `docs/logs/daily/2026-04-21.md`
  - `docs/logs/weekly/2026-W14.md`
- Confirmed the current worktree state for the in-progress `shockwave` slice under
  `tests/fixtures/combat_analysis/s35_sts_catalog_holdout_benchmark_v1/`.
- Added the reviewed fixture
  `s35_ironclad_holdout_shockwave_v1.json` as a new
  `near_neighbor_suppression` case:
  - target holdout: `sts1_ironclad_shockwave`
  - negative neighbor: `sts1_ironclad_thunderclap`
  - shell: `vulnerable_control`
  - mechanism focus: `control + debuff + aoe + bridge/stabilizer`
- Updated the reviewed holdout index and focused toolkit assertions so the pack now
  explicitly includes `6` Ironclad reviewed cases.
- Ran the holdout benchmark script directly and confirmed the expanded pack remains
  fully green:
  - total cases: `6`
  - recovery hit rate: `1.00`
  - structural alignment rate: `1.00`
  - neighbor suppression rate: `1.00`
  - uncertainty-note coverage rate: `1.00`
- Confirmed the new `shockwave` case remains stable with:
  - `recovery_found = True`
  - `best_recovery_rank = 1`
  - top variant
    `sts1_ironclad_intimidate__synth_stability__tune_online`
- Used multi-agent sidecar analysis to refresh expansion priorities:
  - `sts1` remains the only family that is clearly ready for further reviewed holdout
    expansion right now
  - `sts2` still has real source material, but not yet the same reviewed benchmark
    readiness
  - after Ironclad breadth expansion, `sts1_silent` currently looks like the best next
    character lane, ahead of `watcher` and `defect`

## Current Status

- The reviewed `sts1_ironclad_full_v1` holdout lane now has a stable `6`-case baseline.
- This benchmark is now useful as an external capability surface for:
  - holdout recovery
  - near-neighbor suppression
  - shell repair
  - one matchup-sharpened recovery path
- The lane is still exploratory in scope, not proof of autonomous card design.
- The remaining quality gap is mostly about breadth and semantic cleanliness, not about
  the lane being fundamentally broken.

## Later Probe Sweep

- Ran another honest probe round instead of immediately adding more fixtures.
- `sts1_ironclad` follow-up probe results:
  - `pommel_strike` in `strike_matter` failed outright and drifted toward
    `offering`-like gap variants.
  - `second_wind` in `status_shell` and `status_control_test` could technically pass,
    but the recovered shapes drifted toward `iron_wave`, `rage`, `havoc`, and
    `burning_pact` rather than a clean `second_wind` identity.
  - `immolate` in `frontload_aoe`, including a `sts1_reptomancer`-sharpened context,
    still recovered mostly `reaper`-like payoff variants instead of a clean
    `immolate` shape.
  - `uppercut`, `bloodletting`, `body_slam`, and `fiend_fire` remain below the
    "honest reviewed pack" bar from the earlier probe sweep.
- `sts1_silent` starter-lane probe results:
  - `deadly_poison` in `poison_clock` failed.
  - `blur` in `defense_retention` and `acrobatics` in `discard_cycle` could hit the
    formal benchmark bars, but their recovered shapes drifted into `noxious_fumes`,
    `deadly_poison`, `predator`, and other semantically wrong anchors.
  - `blade_dance`, `cloak_and_dagger`, and `poisoned_stab` also did not produce a clean
    enough starter case; the closest survivors still read as off-family variants such as
    `dash`, `survivor`, or `neutralize`.
- Practical conclusion from this round:
  - do not add another reviewed holdout fixture yet
  - keep the stable baseline at `6` Ironclad cases
  - treat the next productive frontier as search/ranking improvement or deeper
    `sts1_silent` readiness work rather than forcing a noisy reviewed expansion

## Probe Notes

- Two sidecar sub-agent probe attempts were started for `sts1_silent` and residual
  `sts1_ironclad` candidates, but both died with upstream `502` errors before returning
  useful results.
- The final conclusions for this round were produced locally from direct holdout-style
  probe runs instead of depending on those sidecars.

## Coverage Inventory

- Recounted the currently available reviewed substrate for external holdout expansion by
  importing the profile modules directly instead of relying on memory.
- `sts1` current support surface:
  - `ironclad`: `16` sample decks, `16` negative decks, `15` near-neighbor cases
  - `silent`: `5` sample, `3` negative, `4` near-neighbor, plus `5` positive matchup,
    `5` negative matchup, and `4` matchup-neighbor cases
  - `watcher`: `5` sample, `2` negative, `4` near-neighbor, plus `5` positive matchup,
    `5` negative matchup, and `4` matchup-neighbor cases
  - `defect`: `4` sample, `1` negative, `3` near-neighbor, plus a wide matchup surface
    (`15` positive / `11` negative / `19` matchup-neighbor) but a thinner stable core
    shell substrate
- `sts2` current support surface:
  - `ironclad`: `5` sample, `3` negative, `4` near-neighbor, and no matchup reviewed
    cases yet
  - `silent`: `4` sample, `3` negative, `4` near-neighbor
  - `defect`: `5` sample, `2` negative, `5` near-neighbor
  - `regent`: `5` sample, `3` negative, `5` near-neighbor
  - `necrobinder`: `4` sample, `3` negative, `5` near-neighbor
- Practical interpretation:
  - `sts1_ironclad` remains the only lane with enough already-validated external
    holdout history to be an active reviewed benchmark today
  - after Ironclad, `sts1_silent` still looks like the cleanest next lane because it has
    a compact but balanced reviewed substrate across shell, contrast, and matchup layers
  - `sts2` is no longer "missing" as a catalog family, but it is still not benchmark-
    ready at the same level because its reviewed evidence is mostly shell/neighbor shaped
    and not yet matchup-rich enough for honest starter holdouts

## Silent Starter Breakthrough

- A deeper `sts1_silent` probe sweep finally produced one reviewed-startup candidate that
  looks clean enough to land rather than just "formally passing":
  - target: `sts1_silent_dash`
  - shell: `frontload_tempo`
  - matchup context: `sts1_gremlin_nob`
  - negative neighbor: `sts1_silent_glass_knife`
- This case is materially stronger than the earlier `deadly_poison`, `blur`,
  `acrobatics`, `blade_dance`, `cloak_and_dagger`, and `poisoned_stab` probes because
  the top recovery band consistently hits:
  - packages: `frontload`, `tempo`, `defense`
  - roles: `bridge`, `stabilizer`, `payoff`
- The strongest recovered variants currently come from `leg_sweep`, `poisoned_stab`, and
  `dagger_throw` mutation families rather than from a fake reappearance of `dash`
  itself, which makes the case useful as an honest holdout benchmark.
- The next implementation slice should therefore be:
  - keep `s35` as the stable Ironclad baseline
  - add a new Silent starter pack instead of forcing more weak Ironclad expansions

## Silent Second-Case Sweep

- After landing the local `dash` starter slice, ran another local probe sweep to see
  whether `sts1_silent` already has a second case clean enough for the same reviewed
  pack.
- Follow-up results:
  - `backflip` in `defense_retention` can technically recover, but the top band drifts
    heavily into `acrobatics` plus one `crippling_cloud` utility shape, so it still
    reads as cross-shell recovery rather than a clean `backflip`-like bridge.
  - `dagger_throw` in `discard_cycle` still fails outright.
  - `neutralize` in `frontload_tempo` still fails outright.
  - `leg_sweep` in `defense_retention` can produce one qualifying variant, but the top
    rankings remain too noisy to trust as a reviewed starter case.
  - earlier `poison_clock`, `blur`, `acrobatics`, `blade_dance`, `cloak_and_dagger`,
    and `poisoned_stab` conclusions remain unchanged: none is clean enough yet.
- Practical conclusion:
  - `sts1_silent_dash_vs_nob` is the first honest Silent starter case
  - a second Silent case is not ready yet
  - the next productive frontier is likely search/ranking improvement for Silent rather
    than more brute-force reviewed fixture authoring

## Sidecar Reliability Note

- A second pair of sidecar probe attempts for the next Silent case also died with
  upstream `502` errors.
- Current Silent conclusions therefore remain based on local direct probe runs rather
  than delegated sub-agent output.

## Watcher Starter Sweep

- Switched expansion focus from "force a second Silent case" to "probe whether another
  external STS character can support a first honest starter lane".
- `sts1_watcher` local starter probes:
  - `third_eye` in `scry_filter_control` versus `cut_through_fate`, sharpened by
    `sts1_sentries`, technically passes but still reads too far from a clean
    `third_eye`-like recovery because the best band drifts into `fear_no_evil` and
    `evaluate`.
  - `vigilance` in `wrath_calm_cycle` versus `tranquility` also technically passes, but
    the recovery collapses too heavily into `fear_no_evil` and does not feel honest
    enough for a starter lane.
  - `wallop` in `block_control_glue` versus `halt`, sharpened by
    `sts1_book_of_stabbing`, is materially cleaner: repeated local probes recover a
    stable `fear_no_evil` / `empty_fist` family with consistent `defense + control +
    aggression` and `bridge + converter + payoff` structure.
- Practical conclusion:
  - `sts1_watcher` now appears to have one honest enough starter case
  - the best first slice is `wallop_vs_book`
  - `third_eye` and `vigilance` should stay probe-only for now

## Validation Reference

- `python scripts/run_sts_catalog_holdout_benchmark.py --input tests/fixtures/combat_analysis/s35_sts_catalog_holdout_benchmark_v1 --output-dir tmp/combat_analysis/sts_catalog_holdout_probe_after_shockwave`
- Inline manual assertion run over the fixture pack and benchmark summary under the
  default local `python` runtime.

## Notes

- In this sandbox, the default `python` runtime can execute the benchmark scripts, but
  `pytest` is not installed there and the external Python 3.11 interpreter is not
  directly executable for full focused pytest runs. For this slice, validation was
  grounded in the direct benchmark run plus a manual assertion script mirroring the
  focused holdout test contract.
- This is acceptable for the current heartbeat slice, but a future environment with a
  runnable pytest interpreter would be better for routine benchmark iteration.
- Re-ran the new starter packs locally after the previous handoff:
  - `s36_sts_catalog_holdout_benchmark_silent_v1` stayed green with top recovery
    `sts1_silent_leg_sweep__synth_gap_payoff__tune_online`.
  - `s37_sts_catalog_holdout_benchmark_watcher_v1` stayed green with top recovery
    `sts1_watcher_fear_no_evil__synth_stability__tune_ceiling`.
  - a manual assertion script rechecked `s35`, `s36`, and `s37` together and confirmed
    benchmark id, catalog id, total-case count, hard-gate metrics, and expected top
    recovery ids for the new Silent and Watcher starter cases.
- The earlier "cannot commit" path in this Codex sandbox also has a git-environment
  component:
  - raw `git status` initially failed on `safe.directory`
  - git commands work when invoked with an explicit
    `-c safe.directory=C:/Users/WINDOWS/.codex/worktrees/fc01/PHD_SIMULATER`
    override, so any remaining commit failure would be a separate filesystem-lock issue
    rather than a benchmark regression.
- Continued the external holdout sweep after the Silent and Watcher starter commits by
  probing `sts1_defect` against the reviewed recommendation contexts instead of forcing
  more noisy shell-only cases.
- The first genuinely clean Defect starter candidate came from the reviewed local
  recommendation context:
  - shell: `dark_delay_shell`
  - matchup context: `sts1_book_of_stabbing`
  - original reviewed patch target: `sts1_defect_cold_snap`
  - primary false-positive control: `sts1_defect_doom_and_gloom`
- This candidate is materially cleaner than the first-wave Defect probes on
  `ball_lightning`, `defragment`, `dualcast`, `doom_and_gloom`, and the early
  `compile_driver` contexts because the top recovery band stabilizes on a consistent
  `glacier` family rather than drifting into unrelated rainbow / recursion / aoe payoff
  shapes.
- The resulting reviewed starter slice is honest but should be described as
  `missing_piece_shell_repair`, not `context_sharpened_recovery`:
  - removing `enemy_intent` did not change the top `glacier` recovery band
  - the important signal is that the reviewed dark-delay shell can still recover a
    frost-control stabilizer after the reviewed `cold_snap` patch card is removed from
    source
  - this is still good benchmark evidence because it exercises external-catalog repair
    under a real reviewed shell + recommendation context rather than a synthetic toy deck
- After adding the local `s38` starter slice, the reviewed external STS holdout surface
  now spans all four currently-modeled `sts1` characters:
  - `s35`: Ironclad (`6` cases)
  - `s36`: Silent (`1` case)
  - `s37`: Watcher (`1` case)
  - `s38`: Defect (`1` case)

## Next Likely Moves

1. Probe the next honest `sts1_ironclad` reviewed candidates before landing more
   fixtures. Current high-value pool is:
   - `uppercut`
   - `bloodletting`
   - `pommel_strike`
   - `body_slam`
   - `fiend_fire`
2. Keep `sts2` out of the reviewed holdout benchmark path until its readiness is at the
   same level as `sts1`.
3. With one starter lane now live for all four `sts1` characters, the next high-value
   frontier is no longer "add any missing starter pack" but one of:
   - a second honest case for `silent`, `watcher`, or `defect`
   - search / ranking improvement where the current top bands still drift semantically
   - additional `ironclad` breadth only if the new cases remain honest under direct local
     outputs
