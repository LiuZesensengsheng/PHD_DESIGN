# Daily Log

- Date: `2026-04-19`

## Session: `codex/04-19-campaign-phase-f`

- Branch: `codex/04-19-campaign-phase-f`

## Today Goals

- Land `Phase E` of `CAMPAIGN_LIFECYCLE_MACHINE_V1` by moving startup/combat
  return aftermath into explicit lifecycle ownership.
- Preserve the existing reward -> meeting deferral UX while removing more
  startup-side compatibility behavior.
- Keep the scope narrow to campaign lifecycle sequencing only.

## Completed

- Landed `RETURN_RESOLUTION` in the lifecycle layer:
  - added `contexts/campaign/lifecycle/steps/return_resolution_steps.py`
  - extended lifecycle context and machine to carry return-transition data and
    execute the dedicated return phase
- Rewired startup return handling so lifecycle owns sequencing instead of the
  startup path assembling it inline:
  - `CampaignRouteResolutionService` now returns explicit startup transitions
  - `CampaignStartupOrchestrator` delegates those transitions into
    `state.resolve_startup_return_transitions(...)`
  - `CampaignState` now routes that call through
    `lifecycle_machine.run_return_resolution(...)`
- Narrowed startup-side compatibility ownership:
  - `campaign_startup_effects_service.py` no longer owns startup reward handoff
  - `campaign_board_mutation_service.py` now exposes a small
    `remove_resolved_route_block(...)` helper instead of the broader old
    startup-return helper path being the mainline owner
- Expanded focused lifecycle/test coverage for the new phase:
  - `tests/campaign/test_campaign_lifecycle_machine.py`
  - `tests/campaign/test_campaign_startup_orchestrator.py`
  - `tests/campaign/test_campaign_dependency_narrowing_services.py`
  - `tests/campaign/test_campaign_ui_state_contracts.py`
- Fixed the main Phase E regression discovered during wider validation:
  - return resolution now still advances the campaign turn whenever a
    transition carries a non-`None` `block_id`, even if the hydrated runtime
    board no longer contains that block
  - this preserves the old startup return semantics and prevents
    reward-choice -> meeting re-entry from getting stuck on turn `0`
- Normalized mixed line endings on the touched Phase E files so
  `tests/shared/test_text_encoding_guards.py` returns to green again.
- Repaired the broken git worktree metadata for this Codex workspace:
  - restored the current worktree HEAD
  - cut a fresh branch `codex/04-19-campaign-phase-f`
  - `git status` now works normally again inside this workspace
- Landed `Phase F` of the lifecycle rollout:
  - added `contexts/campaign/services/campaign_lifecycle_binding_service.py`
  - `CampaignLifecycleHookRegistry` now supports source-owned registrations so
    managed hook sets can be rebuilt safely instead of only appended forever
  - `CampaignState` now exposes `refresh_lifecycle_hooks()` and startup calls
    it before startup return-resolution runs, so `AFTER_COMBAT_RETURN` hooks
    can bind from persisted state before route-return aftermath dispatches
  - reward-side trait acquisition now refreshes lifecycle bindings after the
    trait list changes:
    - `RewardService`
    - `PostCombatRewardFlowService`
    - `MeetingShopService` now also refreshes opportunistically when that seam
      is available
- Moved the first real business reactions onto lifecycle windows:
  - elapsed time label refresh now binds to `AFTER_TURN_ADVANCE`
  - gossip badge increment now binds to `AFTER_FORCED_DDL_REFRESH`
  - line-bubble turn advancement now binds to `AFTER_FORCED_DDL_REFRESH`
- Added focused Phase F tests:
  - `tests/campaign/test_campaign_lifecycle_binding_service.py`
  - updated:
    - `tests/campaign/test_campaign_lifecycle_machine.py`
    - `tests/campaign/test_campaign_turn_transition_steps.py`
    - `tests/campaign/test_campaign_turn_orchestrator.py`
    - `tests/campaign/test_campaign_startup_orchestrator.py`
    - `tests/campaign/test_campaign_state_service_bundle_groups.py`
    - `tests/campaign/test_campaign_dependency_narrowing_services.py`
- Landed the first real `campaign trait` slice on top of the new lifecycle hook substrate:
  - added `contexts/campaign/domain/campaign_trait_catalog.py` as the first explicit campaign-side trait catalog
  - bound real campaign trait semantics through lifecycle windows instead of more orchestrator branches:
    - `trait_introvert` -> `AFTER_COMBAT_RETURN` grants `+1 inspiration`
    - `trait_extravert` -> `AFTER_FORCED_DDL_REFRESH` grants an extra gossip badge
  - meeting-side trait purchase now persists real trait ids instead of the old placeholder text:
    - first purchase -> `trait_introvert`
    - second purchase -> `trait_extravert`
    - later purchases safely stop once the current meeting trait pool is exhausted
  - reward-menu trait choice now also resolves through the same catalog instead of writing placeholder trait text:
    - reward options now omit the trait entry automatically when the current catalog pool is exhausted
    - reward choice payloads now persist real trait ids plus dynamic option ordering metadata
  - post-combat bundle trait rewards now also resolve presentation through the same catalog:
    - bundle trait items now default to catalog-backed title/label/description instead of falling back to raw `trait_id`
    - payload-local `trait_label` / `description` can still override catalog defaults when a specific reward wants custom copy
    - unknown trait ids still degrade safely to `trait_id`-based text instead of failing the bundle
  - added focused coverage for:
    - `tests/campaign/test_campaign_lifecycle_binding_service.py`
    - `tests/campaign/test_meeting_service_core.py`
- Added a dedicated `RETURN_RESOLUTION` flow test pack so the return phase now
  has its own readable sequencing coverage:
  - added `tests/campaign/test_campaign_return_resolution_flow.py`
  - removed the return-resolution-specific assertions from
    `tests/campaign/test_campaign_lifecycle_machine.py` so the generic machine
    test stays focused on phase shells
  - locked the key return contracts explicitly:
    - `AFTER_COMBAT_RETURN` dispatch happens after route cleanup and turn
      advance, but before reward open
    - return resolution still advances the turn and opens the reward even when
      the hydrated runtime board no longer contains the resolved block
    - multiple resolved transitions still advance once per resolved block while
      opening only the first available return reward

## Current Status

- `Phase E` and `Phase F` are now implemented and validated.
- Lifecycle ownership now covers:
  - `TURN_TRANSITION`
  - `TURN_INTERRUPTS`
  - `RETURN_RESOLUTION`
- Lifecycle hook binding is now a real runtime surface instead of a placeholder:
  - system-owned turn reactions are registered through timing windows
  - trait-owned hook slots can now be rebuilt from persisted state without
    editing orchestrators
- `RETURN_RESOLUTION` currently owns:
  - returned route block cleanup
  - return-driven turn advancement
  - `AFTER_COMBAT_RETURN` timing-window dispatch
  - opening the first return reward
- The startup reward -> meeting deferral contract still passes after the move.
- Concrete trait gameplay effects are still intentionally deferred:
  - the binding substrate now exists
  - existing campaign reactions already use it
  - the first real campaign traits now also use it
  - future trait/event reactions can now land as hook registrations instead of
    new orchestrator branches
- Campaign trait work is now partially real rather than placeholder-only:
  - lifecycle-owned trait semantics exist for `trait_introvert` and `trait_extravert`
  - meeting purchase is on real ids with sold-out handling
  - reward-menu trait choice is now also on real ids through the shared catalog
  - post-combat trait bundle presentation is now also on the shared catalog, while still allowing payload-local copy overrides where needed
  - the current campaign trait entry paths are now effectively converged on one catalog seam

## Validation Reference

- `py -3.11 -m pytest tests/shared/test_e2e_headless_reward_modal.py tests/shared/test_e2e_headless_combat_end_ack_to_campaign.py tests/campaign/test_reward_modal_opens_once_and_unlocks_input.py tests/campaign/test_campaign_cleanup_contracts.py -q`
- `py -3.11 -m pytest tests/campaign/test_campaign_lifecycle_machine.py tests/campaign/test_campaign_startup_orchestrator.py tests/campaign/test_campaign_dependency_narrowing_services.py tests/campaign/test_campaign_ui_state_contracts.py -q`
- `py -3.11 -m pytest tests/campaign/test_meeting_prompt_ui_lifecycle.py tests/campaign/test_reward_transition_protocol.py tests/campaign/test_campaign_host_helper_services.py -q`
- `py -3.11 -m pytest tests/campaign/test_campaign_lifecycle_binding_service.py tests/campaign/test_campaign_lifecycle_machine.py tests/campaign/test_campaign_turn_transition_steps.py tests/campaign/test_campaign_startup_orchestrator.py tests/campaign/test_campaign_state_service_bundle_groups.py -q`
- `py -3.11 -m pytest tests/campaign/test_campaign_dependency_narrowing_services.py -q`
- `py -3.11 -m pytest tests/campaign/test_meeting_prompt_ui_lifecycle.py tests/campaign/test_reward_transition_protocol.py tests/campaign/test_campaign_host_helper_services.py tests/campaign/test_reward_modal_opens_once_and_unlocks_input.py tests/campaign/test_campaign_cleanup_contracts.py tests/campaign/test_meeting_service_core.py -q`
- `py -3.11 -m pytest tests/shared/test_e2e_headless_reward_modal.py tests/shared/test_e2e_headless_combat_end_ack_to_campaign.py -q`
- `py -3.11 -m pytest tests/shared/test_text_encoding_guards.py -q`
- `py -3.11 -m pytest tests/campaign -q`
- `py -3.11 -m pytest tests/campaign/test_campaign_lifecycle_binding_service.py tests/campaign/test_meeting_service_core.py -q`
- `py -3.11 -m pytest tests/shared/test_text_encoding_guards.py -q`
- `py -3.11 -m pytest tests/campaign -q`
- `py -3.11 -m pytest tests/campaign/test_reward_transition_protocol.py tests/campaign/test_reward_modal_opens_once_and_unlocks_input.py tests/campaign/test_meeting_prompt_ui_lifecycle.py -q`
- `py -3.11 -m pytest tests/campaign/test_campaign_lifecycle_binding_service.py tests/campaign/test_meeting_service_core.py -q`
- `py -3.11 -m pytest tests/shared/test_text_encoding_guards.py -q`
- `py -3.11 -m pytest tests/campaign -q`
- `py -3.11 -m pytest tests/campaign/test_campaign_lifecycle_binding_service.py -q`
- `py -3.11 -m pytest tests/shared/test_text_encoding_guards.py -q`
- `py -3.11 -m pytest tests/campaign/test_reward_transition_protocol.py tests/campaign/test_reward_modal_opens_once_and_unlocks_input.py tests/campaign/test_meeting_prompt_ui_lifecycle.py -q`
- `py -3.11 -m pytest tests/campaign -q`
- `py -3.11 -m pytest tests/campaign/test_campaign_return_resolution_flow.py tests/campaign/test_campaign_lifecycle_machine.py -q`
- `py -3.11 -m pytest tests/shared/test_text_encoding_guards.py -q`
- `py -3.11 -m pytest tests/campaign -q`

## Next Likely Moves

1. Decide the first non-placeholder campaign trait or event family to bind onto
   the new lifecycle hook substrate.
2. Expand the campaign trait catalog beyond the current two-trait seed so more
   acquisition paths can stay catalog-native without immediately falling into
   sold-out behavior.
3. Decide whether the next trait slice should stay purely passive or start
   adding one interrupt/gate-affecting trait family on the lifecycle substrate.
4. Delete the remaining return-resolution compatibility residue once the team
   is comfortable removing the old broader helper path completely.
5. Expand `RETURN_RESOLUTION` flow coverage again once that phase starts owning
   another real interrupt family beyond reward open.

## Session: `codex/04-18-module-architecture-review`

- Branch: `codex/04-18-module-architecture-review`

## Today Goals

- Finish the current combat runtime cleanup slice around `TurnFlowOrchestrator`.
- Remove the last three runtime-mainline `CombatModel` private seam dependencies without touching `combat_view`, save/schema contracts, or combat content semantics.
- Record the immediate follow-up plan in project memory before opening a PR.

## Completed

- Re-read the active project direction, default entrypoints, decision log, and recent memory before continuing the combat refactor.
- Validated the in-progress combat runtime refactor before adding more changes:
  - compiled all touched combat runtime files
  - ran focused regression packs around:
    - `turn_flow_orchestrator`
    - timing contract
    - heart demon behavior
    - post-resolution policy
    - runtime host migration
    - runtime surface inventory
- Finished the current host-migration slice so combat mainline no longer depends on these `CombatModel` private seams:
  - `_inject_turn_start_environment_cards(...)`
  - `_decay_player_block_at_turn_start(...)`
  - `_inject_heart_demons(...)`
- Split enemy-turn housekeeping out of the older `TurnFlowOrchestrator` body into explicit collaborators:
  - `EnemyTurnStartPolicy`
  - `EnemyTurnEndPolicy`
  - `EnemyBlockDecayPolicy`
- Moved player turn-start ownership onto explicit runtime collaborators:
  - `PlayerBlockDecayPolicy`
  - `DefaultTurnStartEnvironmentInjector`
- Moved heart-demon injection / cleanup ownership onto `HeartDemonService`, then rewired:
  - `IdealPolicy`
  - `CombatPostResolutionPolicy`
  - `CombatSession`
- Kept `CombatModel` as an explicit compatibility facade, but only as a wrapper layer rather than a mainline runtime dependency.
- Updated the timing-contract test to patch the new runtime injector entrypoint instead of the old model-private helper seam.
- Added a new guard test so the three removed runtime seams cannot silently re-enter combat mainline.
- Updated the combat runtime surface inventory doc so it matches the actual code state after this slice.
- Removed the first low-risk batch of remaining `CombatModel` compat wrappers:
  - `_update_enemy_turn(...)`
  - `_check_combat_end()`
  - `_process_chore_host_enemy_turn_start()`
- Moved their surviving callers to explicit runtime ownership:
  - enemy-turn drain tests now use `model.session._update_enemy_turn(...)`
  - combat-end tests now use `model.session.post_resolution_policy.check_combat_end(...)`
  - chore-host enemy-turn-start tests now use `model.session.turn_flow_orchestrator.on_enemy_turn_started(...)`
- Removed the remaining `CombatModel` private method wrappers that were only serving tests/legacy compatibility:
  - `_handle_after_card_play(...)`
  - `_apply_chore_resolution_actions(...)`
  - `_prune_defeated_enemies_and_adjust_pointer()`
  - `_inject_turn_start_environment_cards(...)`
  - `_handle_card_played(...)`
  - `_decay_player_block_at_turn_start(...)`
  - `_inject_heart_demons(...)`
- Removed the last presentation-shaped `CombatModel` private compatibility properties:
  - `_phase_banner`
  - `_enemy_action_banner`
- After this pass, the active private `CombatModel` compatibility backlog is `0`.
- Started the next combat runtime shape cut:
  - `CombatSession.play_card(...)` is now the explicit card-play command pipeline owner
  - `PlayCardTransaction` now stops at local commit/cleanup instead of driving orchestration through event-bus publish
  - `CardPlayed` is kept only as a post-resolution fact broadcast and no longer acts as the runtime trigger for `CardPlayOrchestrator`
  - added an allowlist guard so mainline card play cannot silently fall back to `subscribe(CardPlayed, ...)`
- Started the next `CombatSession` host-thinning cut on runtime data:
  - introduced `CombatSessionRuntimeState` as the dedicated host for session-local mutable runtime data
  - moved phase/banner/enemy-step runtime fields behind that data host
  - kept session compatibility properties so render/tests do not need to migrate in the same slice
- Continued the next `CombatSession` host-thinning cut on startup ownership:
  - moved combat startup sequencing out of `CombatSession.start_combat()` into `CombatStartOrchestrator`
  - kept `CombatSession.start_combat()` as the stable runtime entrypoint while relocating:
    - starter deck bootstrapping
    - combat-start reaction dispatch
    - opening shuffle / innate move / opening draw
    - first-player-turn entry handoff
  - added a focused startup orchestration test so timing/order regressions fail close to the new owner
- Continued the next `CombatSession` host-thinning cut on judgment event handling:
  - moved `StressThresholdReached -> judgment` handling out of `CombatSession` into `StressThresholdHandler`
  - kept the session as the event-wiring host only:
    - subscribe `StressThresholdReached`
    - delegate the event body to the named handler
  - added focused handler tests for:
    - successful `JudgmentResolved` publish
    - failure path combat-loss marking
- Continued the next `CombatSession` host-thinning cut on enemy-turn sequence driving:
  - moved enemy-turn timed-step ownership out of `CombatSession` into `EnemyTurnRuntimeDriver`
  - kept the phase machine / session entrypoints stable while relocating:
    - enemy turn enter sequencing
    - step start progression
    - enemy action banner updates
    - synchronous drain-to-completion helper path
- Continued the next `CombatSession` host-thinning cut on presentation shaping:
  - moved banner/event shaping and command-gate facade delegation behind `CombatPresentationRuntime`
  - kept `CombatPresentationBridge` as the lower-level lease/log/consumer owner
  - kept session compatibility methods/properties stable by delegating into the new runtime collaborator
- Continued the next `CombatSession` host-thinning cut on composition-root narrowing:
  - moved default collaborator construction out of `CombatSession.__init__` into `build_default_combat_session_services(...)`
  - introduced `CombatSessionServices` as the explicit runtime collaborator bundle
  - reduced `CombatSession.__init__` from inline assembly logic to:
    - session state/event bus setup
    - service bundle attach
    - runtime wiring / default arena effect attach

## Current Status

- The current combat runtime migration hit its planned milestone for this slice:
  - `CombatSession` remains the canonical runtime host
  - `CombatModel` remains an explicit compatibility facade
  - runtime-mainline ownership is off the last three targeted host-private seams
- The startup mainline is now also off the `CombatSession` body itself:
  - `CombatSession.start_combat()` is reduced to an explicit delegation entrypoint
  - startup sequencing ownership now lives in `CombatStartOrchestrator`
- The stress-threshold judgment path is now also off the session host body:
  - `CombatSession` only wires the subscription
  - `StressThresholdHandler` owns the judgment event flow
- The enemy-turn timed sequence is now also off the session host body:
  - `CombatSession` delegates enemy-turn progression to `EnemyTurnRuntimeDriver`
- Presentation shaping and gate delegation are now explicit:
  - `CombatPresentationRuntime` owns banner/event payload shaping on top of the lower-level bridge
- Default runtime collaborator assembly is no longer embedded in `CombatSession.__init__`:
  - the session now attaches a prebuilt `CombatSessionServices` bundle
- The active backlog is no longer "production orchestration still depends on old host helpers".
- The active backlog is now mostly test/compat cleanup:
  - runtime-owned transitional seam count for this slice is effectively `0`
  - active private `CombatModel` compat wrapper backlog is now `0`
  - the explicit private compat backlog dropped from `12` to `0`
- The refactor stayed within the agreed safe boundary:
  - no `combat_view` behavior change
  - no combat content semantics change
  - no save/schema contract change

## Validation Reference

- `py -3.11 -m py_compile contexts/combat/application/orchestration/enemy_block_decay_policy.py contexts/combat/application/orchestration/enemy_turn_end_policy.py contexts/combat/application/orchestration/enemy_turn_start_policy.py contexts/combat/application/orchestration/player_block_decay_policy.py contexts/combat/domain/services/heart_demon_service.py contexts/combat/domain/arena_effects.py contexts/combat/application/orchestration/player_turn_orchestrator.py contexts/combat/application/orchestration/turn_flow_orchestrator.py contexts/combat/application/orchestration/post_resolution_policy.py contexts/combat/domain/services/ideal_policy.py contexts/combat/runtime/session.py contexts/combat/mvc/model.py`
- `py -3.11 -m pytest tests/combat/test_turn_flow_orchestrator.py tests/combat/test_combat_timing_contract_v1.py tests/combat/test_heart_demons.py tests/combat/test_post_resolution_policy.py tests/combat/test_combat_runtime_host_migration_v1.py tests/combat/test_combat_runtime_surface_inventory_v1.py -q`
- `py -3.11 -m pytest tests/combat -q`
- `py -3.11 -m pytest tests/simulation -q`
- `py -3.11 -m pytest tests/save/test_combat_save_service.py -q`
- `py -3.11 -m py_compile contexts/combat/application/orchestration/combat_start_orchestrator.py contexts/combat/application/orchestration/__init__.py contexts/combat/runtime/session.py tests/combat/test_combat_start_orchestrator.py`
- `py -3.11 -m pytest tests/combat/test_combat_start_orchestrator.py tests/combat/test_combat_timing_contract_v1.py tests/combat/test_combat_phase_machine.py tests/combat/test_combat_mainline_smoke_pack_v1.py tests/combat/test_combat_runtime_host_migration_v1.py -q`
- `py -3.11 scripts/check_combat_compat_zero.py`
- `py -3.11 -m py_compile contexts/combat/application/orchestration/stress_threshold_handler.py contexts/combat/application/orchestration/__init__.py contexts/combat/runtime/session.py tests/combat/test_stress_threshold_handler.py`
- `py -3.11 -m pytest tests/combat/test_stress_threshold_handler.py tests/combat/test_judgment_minimal.py tests/combat/test_combat_start_orchestrator.py tests/combat/test_combat_timing_contract_v1.py tests/combat/test_combat_phase_machine.py tests/combat/test_combat_mainline_smoke_pack_v1.py tests/combat/test_combat_runtime_host_migration_v1.py -q`
- `py -3.11 scripts/check_combat_compat_zero.py`
- `py -3.11 -m pytest tests/combat/test_combat_presentation_contract_v1.py tests/simulation/test_headless_executor.py -q`
- `py -3.11 scripts/check_combat_compat_zero.py`
- `py -3.11 -m py_compile contexts/combat/runtime/presentation_runtime.py contexts/combat/runtime/enemy_turn_runtime_driver.py contexts/combat/runtime/session_services.py contexts/combat/runtime/session_runtime_state.py contexts/combat/runtime/session.py contexts/combat/runtime/__init__.py tests/combat/test_enemy_turn_runtime_driver.py tests/combat/test_presentation_runtime.py tests/combat/test_session_services.py`
- `py -3.11 -m pytest tests/combat/test_enemy_turn_runtime_driver.py tests/combat/test_presentation_runtime.py tests/combat/test_session_services.py -q`
- `py -3.11 -m pytest tests/combat/test_combat_start_orchestrator.py tests/combat/test_stress_threshold_handler.py tests/combat/test_combat_timing_contract_v1.py tests/combat/test_combat_phase_machine.py tests/combat/test_combat_mainline_smoke_pack_v1.py tests/combat/test_judgment_minimal.py tests/combat/test_combat_presentation_contract_v1.py tests/combat/test_combat_runtime_host_migration_v1.py tests/simulation/test_headless_executor.py -q`
- `py -3.11 -m pytest tests/combat/test_combat_runtime_surface_inventory_v1.py tests/combat/test_combat_runtime_host_migration_v1.py -q`
- `py -3.11 -m pytest tests/save/test_combat_save_service.py -q`
- `py -3.11 scripts/check_combat_compat_zero.py`

## Observed Unrelated Failures

- `py -3.11 -m pytest tests/combat -q` still reports `6` existing failures in `tests/combat/test_json_red_cards.py`
- those failures are not in the current `CombatSession` host-thinning slice; they align with the existing queue-only planning/content backlog around mixed-color/red paths that still hit unplanned `reposition` effects without explicit `target_id`
- the repo docs already describe that `reposition` is explicit-target-only in mainline:
  - `docs/development/COMBAT_FALLBACK_AND_SHIM_INVENTORY_V1.md`
  - `docs/development/COMBAT_MAINLINE_GATE_V2.md`

## Next Likely Moves

1. Continue shrinking the test-only `CombatModel` compat surface instead of adding new runtime wrappers.
2. Start with the safest remaining compat seams:
   - no private `CombatModel` compat seams remain in the active inventory
3. Move tests and legacy callers toward direct `session` or named collaborators where doing so makes ownership clearer.
4. Decide seam-by-seam whether the remaining wrappers should:
   - stay as intentional compatibility surface for one more phase
   - or be hard-deleted after callers migrate
5. Keep real animation/video gating out of this slice for now:
   - retain placeholder command-lock / presentation interfaces
   - wait for visual/runtime requirements before making animation blocking part of the authoritative combat flow
6. Keep avoiding `combat_view`, contract schema, and content semantics changes while the runtime host is still being tightened.
7. After the enemy-turn / presentation / composition-root cuts in this pass, the next optional runtime cleanups are lower priority:
   - save/load facade extraction if we want an even thinner host
   - trimming how many collaborators remain explicitly attached on the session surface
   - red content/mainline planning backlog is now more pressing than further session shaving

## Follow-Up Guidance

- Future combat runtime work should keep following the same safe constraints used in this slice:
  - prefer explicit collaborator ownership over generic host-private helpers
  - avoid touching `combat_view` unless the slice is explicitly visual
  - avoid content or schema changes while finishing runtime host cleanup
- If a future effect family still cannot fit queue/planner contracts cleanly, treat that as a modeling signal rather than a reason to restore a silent fallback seam.

## Session: `codex/04-18-cardanalysis-dataset-expansion`

- Branch: `codex/04-18-cardanalysis-dataset-expansion`
- Key commits:
  - `e973fc0` `Add high-value loop retain and power benchmark cases`
  - `b756161` `Expand deck fun benchmark and candidate fun deltas`

## Today Goals

- Record the next combat-analysis iteration plan after the latest `deck_fun_benchmark_v1` expansion.
- Preserve an honest resume point before starting generator-adjacent work.
- Push the current branch and open a PR if GitHub auth allows it.

## Completed

- Re-read the active project memory docs before writing new handoff state.
- Confirmed the reviewed `deck_fun_benchmark_v1` pack is now:
  - `60` cases total
  - balanced at `15 / 15 / 15 / 15` across both character coverage and `heat x strength` quadrants
  - still `low = 0` on `confidence_band`
- Confirmed the current evaluator progress is strongest on:
  - reviewed card/package/deck boundary calibration
  - fast-lane candidate ranking with explicit `fun_delta`
  - clearer `fun / feel` contrasts around:
    - `clear loop vs almost loop`
    - `clean retain shell vs retain hybrid soup`
    - `clear power lock vs good-card power soup`
- Confirmed the current honest boundary is still:
  - strong assisted design iteration
  - not yet autonomous card generation or closed-loop balance solving
- Added a first constrained synthesis bridge on top of the current fast lane:
  - new `fast_card_synthesis_bridge` flow now selects seed cards from mechanism-axis intent, emits bounded skeleton variants, and re-scores them through the existing fast card loop
  - variants stay projection/skeleton level on purpose; the bridge does not pretend exact runtime text/effect authoring is solved
- Recorded direct commands for the new bridge in `docs/development/DEFAULT_ENTRYPOINTS.md`.
- Added focused toolkit + script coverage for the new bridge and kept the existing fast-card-loop tests green.
- Added a bounded local numeric tuner on top of the synthesis bridge:
  - base variants are evaluated first, then promising variants get local tuned follow-ups
  - tuned variants keep explicit `variant_stage`, `parent_variant_id`, and `tuning_focus` metadata
  - tuning stays honest and local:
    - it reuses existing `fast_card_loop` balance flags
    - retain-like support is expressed as timing-window projection notes
    - exhaust-like tradeoffs stay explicit as projection gaps / risk notes instead of pretending full runtime certainty
- Extended the synthesis bridge report and snapshot outputs so base/tuned counts and tuned lineage show up directly in artifacts.
- Validated the tuner pass end to end:
  - focused synthesis + fast-card-loop tests passed
  - neighboring draft/project/encoding checks passed
  - full `py -3.11 -m pytest -q` passed after CRLF normalization on touched synthesis files
- Added a first synthesis closure sidecar on top of the bridge + tuner:
  - it runs generated ranked variants back through `fun_proxy`
  - it carries the reviewed `deck_fun_benchmark_v1` calibration context into the closure report so direct-neighbor / partial-neighbor / blind-spot claims stay explicit
  - it replays generated variants inside matching `project_card_design_v1` reviewed fixture intents instead of pretending the original reviewed expectation list can grade out-of-set generated cards directly
- Recorded direct commands for the new closure sidecar in `docs/development/DEFAULT_ENTRYPOINTS.md`.
- Added focused toolkit + script coverage for the closure sidecar and kept adjacent synthesis/fun-proxy tests green.
- Ran a real local closure probe from:
  - `tmp/combat_analysis/synthesis_closure_probe/input.json`
  - output dir: `tmp/combat_analysis/synthesis_closure_probe/generated`
- Confirmed the first real closure report is already surfacing actionable cluster patterns instead of only raw tables:
  - `single_base_card_dominance`: the ranked slice is still collapsing onto `red_chaos_draft`
  - `tuning_focus_dominance`: `retain_support` is the real winning branch in this probe, but it is also over-dominating the local search
  - `weak_local_repair_branch`: `red_chaos_draft__synth_floor__tune_online` is a low-value repair branch, not a promising local improvement
  - `replay_top_variant_collapse` plus `replay_ranking_collapse`: all reviewed replay envelopes keep the same winner and the same ranked order
  - `balance_flag_replay_persistence`: the top replay winner still survives balance-guardrail replays with balance flags attached
- Confirmed the important honest nuance from parent deltas:
  - the retain-tuned variants are not fake wins; they improved both fast-card score and proxy score against their parents
  - the current `tune_online` branch is the opposite shape: almost no fast-card lift and a proxy drop
- Extended the closure cluster output so each cluster now carries an explicit follow-up note for the next reviewed-sample slice instead of only describing the failure pattern.
- Added a first reviewed red `matchup_elasticity` sibling trio to `project_card_design_v1`:
  - same current deck
  - same candidate trio: `red_defense_and_strike`, `red_double_lottery`, `red_mark_next_frontier`
  - different targets now intentionally reorder the winner:
    - `sts1_gremlin_nob` -> `red_defense_and_strike`
    - `sts1_awakened_one` -> `red_mark_next_frontier`
    - `sts1_sentries` -> `red_double_lottery`
- Confirmed those new reviewed fixtures are now consumed by the closure sidecar as additional replay envelopes:
  - red-project replay count moved from `6` to `9`
  - the current generated retain variants still collapse to the same replay winner even under the richer fixture set, which is useful evidence that the ranking-collapse problem is real rather than under-sampled
- Added the next "second class" reviewed synthesis assertions directly on top of existing project-card fixtures so non-`retain_support` winners are now locked in three distinct contexts:
  - `balance_guardrail`: `red_scribble_note__synth_payload__tune_exhaust`
  - `enemy_matchup` versus `sts1_awakened_one`: `red_defense_and_strike__synth_gap_enabler__tune_exhaust`
  - `mixed mechanism_axis`: `a0b1c2d3-e4f5-6789-abcd-00000000010d__synth_payload__tune_exhaust`
  - each assertion also requires real `retain_support` variants to exist in the ranked slice and lose on score, so the contrast is not a fake no-op "non-retain wins because retain never appeared" case

## Current Blockers

- `fast_card_loop` is still a closed-set ranker:
  - it requires both `current_deck_card_ids` and `candidate_card_ids`
  - it does not yet synthesize new card candidates by itself
- The new synthesis bridge is still bounded and honest:
  - it mutates projection-level skeletons, not runtime card JSON/text
  - exhaust-like tradeoffs are surfaced as explicit synthesis notes/risk notes rather than fully calibrated long-run persistence math
- Numeric balancing is still threshold-only:
  - bootstrap / online / dead-card / energy-strain checks exist
  - the new tuner can only do bounded local search over reviewed projection knobs
  - there is still no full balance oracle, long-run simulation, or autonomous numeric sign-off
- `fun_proxy` still has incomplete coverage for:
  - `matchup_elasticity` as a real blind spot
  - `variance_reliance` as a partial neighbor only
  - `degeneracy_risk` as a partial neighbor only
- The new closure sidecar is still an evaluation harness, not a final judge:
  - fixture replay is contextual and reviewed, but not a hard pass/fail contract for generated variant ids
  - `fun_proxy` still cannot honestly close `matchup_elasticity`, and only partially neighbors `variance_reliance` / `degeneracy_risk`
- `Design Loop v1` and the fast-lane workflows still depend on reviewed structured input, not raw card text or autonomous proposal generation.

## Next Step

1. Use the closure follow-up notes to drive the next reviewed sample expansion instead of guessing.
   - Prioritize `matchup_elasticity` cases where replay context should reorder at least the top two generated variants.
   - Add non-`retain_support` contrast cases so the tuner has reviewed situations where another repair family should win.
   - Add stronger balance-guardrail envelopes that reward safe shell quality over raw fast-card score.
2. Reduce the remaining fun/fidelity blind spots before claiming more autonomy.
   - Keep `matchup_elasticity` as the top blind-spot target.
   - Keep `variance_reliance` and `degeneracy_risk` as the next partial-neighbor tightening targets.
   - Use `weak_local_repair_branch` examples as contrast negatives for `expression_clarity` / soup-boundary work rather than as more seeds to amplify.
3. Keep explicitly out of scope for the immediate next slice:
   - open-library retrieval or generation
   - direct numeric balance sign-off
   - full run simulation

## Candidate Long-Term Memory

- No promotion yet.
- The current plan is still execution-local and should stay in short-term memory until the constrained synthesis bridge is real and stable enough to write into longer-term subsystem docs.
