# Daily Log

- Date: `2026-05-07`
- Active branches:
  - `codex/05-07-mechanism-axis-search-bundle`
  - `codex/05-07-cardanalysis-300-case-expansion`
  - `20260507_test`
- Note: this log was compressed after merging `origin/master` into `20260507_test`
  so the hot-memory daily log stays recovery-sized.

## Today Goals

- Add a report-only mechanism-axis search bundle that lets the program enumerate,
  score, and explain candidate mechanism axes instead of relying on an LLM to list
  axes from memory.
- Extend that mechanism-axis output into a bounded report-only downstream chain:
  design brief, package seed, evaluation handoff, owner report requests, owner input
  readiness, generation-exam readiness, owner evidence queue, and empty evidence
  intake templates.
- Continue cardanalysis evidence expansion in fixture-role-specific lanes without
  dumping every new case into one library.
- Preserve advisory boundaries: no runtime changes, no formal card/enemy data, no UI
  or save changes, no hard gates, no default synthesis, and no learned/reranker
  promotion.

## Completed

- Added `mechanism_axis_search_bundle_v1`:
  - enumerates candidate axes from `mechanism_axis_contracts.py`;
  - pulls STS1 axis-map evidence, mechanism-axis viability fixtures, deck-fun
    fixtures, and foundation-axis taxonomy language;
  - emits candidate axes, score components, overall scores, support axes, rejected
    axes, evidence refs, and risk notes.
- Added the first Silent request fixture:
  - `tests/fixtures/combat_analysis/mechanism_axis_search_v1/silent_high_agency_visible_clock_v1.json`.
- Added the mechanism-axis report-only downstream chain:
  - `mechanism_axis_design_brief_v1`;
  - `mechanism_axis_package_seed_v1`;
  - `mechanism_axis_report_only_chain_v1` test coverage;
  - `mechanism_axis_evaluation_handoff_v1`;
  - `mechanism_axis_owner_report_requests_v1`;
  - `mechanism_axis_owner_report_input_plan_v1`;
  - `mechanism_axis_generation_exam_readiness_v1`;
  - `mechanism_axis_owner_evidence_queue_v1`;
  - `mechanism_axis_owner_evidence_intake_packet_v1`.
- Recorded the stable commands in:
  - `docs/development/DEFAULT_ENTRYPOINTS.md`;
  - `docs/development/cardanalysis/MECHANISM_AXIS_SEARCH_BUNDLE_V1.md`.
- Added cardanalysis 300-case expansion batches in role-specific lanes:
  - mechanism package completion and conflict fixtures;
  - source-followup package rounds 4 and 5;
  - campaign-phase recovery and bridge extension fixtures;
  - enemy-archetype recovery-gap and bridge fixtures;
  - card-package-health fixtures for shiv bridge, retain payoff-only, poison/shiv
    overstuffing, and brittle dark-release exactness;
  - campaign advisory bundle expansion;
  - autonomous design review fixtures for generic-goodstuff blur, fail-state gap, and
    campaign advisory conflict context;
  - campaign experience curve expansion.
- Resolved the later `20260507_test` merge conflict with `origin/master` in
  `scripts/check_text_encoding.py` by keeping both the pre-reorg allowlist entries
  and the post-reorg architecture-doc allowlist entry.

## Current Blockers

- The mechanism-axis chain remains blocked before any card-package generation exam
  until real owner or human evidence fills the intake templates and canonical owner
  reports exist.
- The cardanalysis expansion remains advisory until explicit human review promotes
  any evidence tier.
- No current Git conflict remains after the `20260507_test` merge resolution.

## Validation

- Mechanism-axis focused tests passed during the branch work:
  - search, design brief, package seed, chain exam, evaluation handoff, owner request,
    owner input plan, generation readiness, owner evidence queue, and owner evidence
    intake packet tests.
- Cardanalysis expansion focused tests passed for the touched fixture lanes during
  merge validation.
- Guardrails run during the merge/conflict resolution:
  - `python scripts/check_text_encoding.py`;
  - `python scripts/validate_architecture.py`;
  - `python scripts/validate_capability_graph.py`;
  - `git diff --check`.

## Next Step

- For mechanism-axis work, fill one owner evidence intake template only from real
  traceable owner or human material, then consider a separate adapter that writes
  canonical owner input files.
- For cardanalysis evidence expansion, continue adding small fixture batches by
  consumer surface and keep them `advisory_context_only` unless reviewed.
- For `20260507_test`, finish the merge commit after validation and push the updated
  branch.

## Candidate Long-Term Memory

- `mechanism_axis_search_bundle_v1` is the program-owned source of candidate axis
  enumeration and scoring; downstream LLM work may only explain output or write
  constrained briefs.
- The mechanism-axis package path is report-only and explicitly blocked before
  generation until owner evidence and canonical owner reports exist.
- Daily logs should stay as compact handoff notes. Detailed same-day execution history
  belongs in git history, stable docs, or weekly/monthly summaries once it stops
  changing.
