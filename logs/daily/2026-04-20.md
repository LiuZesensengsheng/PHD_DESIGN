# Daily Log

- Date: `2026-04-20`
- Branch: `codex/04-19-campaign-phase-f`

## Today Goals

- Finish the first-tier campaign compatibility cleanup that was left mid-flight.
- Remove remaining dead startup/runtime-ui compatibility seams without reopening
  broader campaign refactors.
- Revalidate campaign after the fail-fast startup hydration change.

## Completed

- Finished the first-tier campaign compat-zero cleanup already in progress:
  - removed dead `CampaignState` startup wrappers such as:
    - `resolve_startup_return_transitions(...)`
    - `maybe_show_startup_meeting_prompt(...)`
    - `end_turn()`
    - startup board-checkpoint / thesis-blueprint wrappers
  - renamed the return reward seam to `open_return_reward(...)`
  - removed old `CampaignView` runtime-ui no-op hooks:
    - `handle_runtime_ui_event(...)`
    - `update_runtime_ui(...)`
    - `render_runtime_ui(...)`
- Tightened startup hydration failure contracts:
  - restored combat blocks now require explicit `encounter_id`
  - invalid snapshot restore now fails fast instead of silently restoring an
    empty board/runtime
  - legacy combat encounter-id backfill was removed
- Kept startup orchestration narrow but explicit:
  - startup now refreshes lifecycle hooks, hands startup return transitions to
    lifecycle-owned `RETURN_RESOLUTION`, then rechecks interrupt gates
  - startup effects now only own initial board seed and pending meeting
    add-line application
- Repaired the remaining regression from the cleanup:
  - `tests/campaign/test_campaign_state_modal_event_consumption.py` now installs
    a real `startup_services.frame_orchestrator` stub path instead of depending
    on removed fallback construction inside `CampaignState`
- Normalized line endings on the touched files so text/encoding guardrails
  return to green after the interrupted edit batch.
- Updated architecture docs to match the new baseline:
  - `CAMPAIGN_STARTUP_SEAM_CLEANUP_V1.md`
  - `CAMPAIGN_END_TURN_ORCHESTRATOR_V1.md`
  - `CAMPAIGN_RUNTIME_UI_BOUNDARY_V1.md`
  - `CAMPAIGN_SERVICE_DEPENDENCY_HOTSPOTS_V1.md`
  - `CAMPAIGN_TRANSITION_REQUEST_CONTRACT_V1.md`
  - `CAMPAIGN_UI_INTERACTION_CONTRACT_V1.md`
- Continued the campaign compat cleanup beyond the original first-tier batch:
  - deleted the private `CampaignState._unlock_gossip_inputs()` helper
  - `GossipFlowApplicationService` now routes lock acquire/release and owner
    reads through `ModalCoordinator` only
  - `ThesisJudgmentFlowService` no longer keeps a raw `_input_locked` fallback
    release path
  - narrowed service protocols so `ThesisJudgmentFlowHost` no longer requires
    raw modal-lock fields
- Removed another small state compat seam from task-area fusion:
  - `TrackBlockService` fused block ids now allocate through
    `allocate_campaign_block_id()`
  - the old `CampaignState._next_id()` helper and synthetic fused-id fallback
    were deleted
- Finished the next modal / meeting prompt compat cleanup slice:
  - `CampaignInputLockService` now reads lock ownership through
    `ModalCoordinator` and meeting-prompt UI identity/hit-test state through
    `MeetingPromptUiService`
  - `interrupt_gate_steps._meeting_gate()` now checks visibility through
    `meeting_prompt_ui.is_visible()` and lock ownership through
    `modal.current_owner()`
  - `CampaignPointerHoverService`, `CampaignUiButtonEventService`, and
    `CampaignKeyboardEventService` no longer read raw meeting prompt button /
    hover fields directly
  - added guardrails so those service/lifecycle files cannot reintroduce direct
    reads of meeting-prompt raw fields or modal-lock raw fields
- Added new guardrails so these seams do not reappear:
  - no direct modal-lock fallback writes in gossip/judgment services
  - no `_unlock_gossip_inputs` private seam
  - no `_next_id()` or synthetic fused-id fallback in `TrackBlockService`

## Current Status

- The first-tier campaign compatibility cleanup is now functionally landed in
  the worktree.
- Campaign startup no longer pretends old unpublished save shapes are valid.
- `CampaignView` is no longer carrying generic runtime-ui compatibility hooks.
- `CampaignState` no longer carries the removed startup/compat wrapper layer for
  return resolution, startup meeting prompt, or old end-turn bridging.
- Modal-lock writes are now more centralized:
  - `ModalCoordinator` remains the owner-aware write path
  - gossip/judgment service-side raw fallback writes are removed
- Modal-lock and meeting-prompt reads are also narrower now:
  - active services use `ModalCoordinator` / `MeetingPromptUiService` query
    seams instead of reading raw owner/button/window fields directly
- Task-area fusion no longer depends on the old `_next_id()` shell helper.
- Focused and broad campaign validation are green again after the cleanup.

## Validation Reference

- `py -3.11 -m pytest tests/campaign/test_campaign_state_modal_event_consumption.py -q`
- `py -3.11 -m pytest tests/shared/test_text_encoding_guards.py -q`
- `py -3.11 -m pytest tests/campaign -q`
- `py -3.11 -m pytest tests/campaign/test_gossip_flow_app_service.py tests/campaign/test_gossip_ui_lifecycle.py tests/campaign/test_thesis_judgment_flow_service.py tests/campaign/test_campaign_simplification_guardrails_v1.py tests/campaign/test_campaign_modal_lock_contract.py tests/campaign/test_campaign_host_helper_services.py tests/campaign/test_campaign_ui_state_contracts.py -q`
- `py -3.11 -m pytest tests/campaign/test_campaign_keyboard_event_service.py tests/campaign/test_track_block_service_ddl.py tests/campaign/test_campaign_combat_encounter_contract.py tests/campaign/test_campaign_simplification_guardrails_v1.py -q`
- `py -3.11 -m pytest tests/campaign/test_campaign_input_lock_service.py tests/campaign/test_campaign_pointer_hover_service.py tests/campaign/test_campaign_ui_button_event_service.py tests/campaign/test_campaign_keyboard_event_service.py tests/campaign/test_meeting_prompt_ui_service.py tests/campaign/test_campaign_lifecycle_machine.py tests/campaign/test_campaign_simplification_guardrails_v1.py -q`
- `py -3.11 -m pytest tests/shared/test_text_encoding_guards.py -q`
- `py -3.11 -m pytest tests/campaign -q`

## Next Likely Moves

1. Continue the compatibility scan on the remaining campaign startup/state
   seams and remove any leftover dead wrapper surfaces that are no longer used.
2. Decide whether the next cleanup batch should target deeper raw
   `state._xxx` reads in mature services or a dedicated modal-lock storage
   consolidation pass.
3. Scan the remaining campaign compat surfaces such as service-bundle alias
   carryovers, reward dual-path seams, and old-save preservation seams, then
   rank the next low-risk cleanup batch.
